# Current state and open decisions

State claims rot. This page names where truth lives and which decisions are genuinely open; when it conflicts with the tree, the tree wins — then update this page.

## Where truth lives

- **B-60 — Read state, don't recall it.** System behavior: `pytest --spec`. Backlog and order: `TODO.md` + `stories/*/*.yaml` status fields. Repair backlog: `.coordination/verified-residue.md` — the stamped, re-runnable defect list (each item carries its check command); consult it there, do not duplicate it. Session/phase state of the 2026 reevaluation: the HANDOFF section at the top of `.coordination/threads.md`. Domain reference: `docs/apple_books.md` with the epistemic hedges from the domain page applied.

## Open author decisions — record, do not resolve

- **B-61 — The dead plist API.** `library.list_books` / `find_book_by_title` are integration-tested but have zero callers in `cli.py`, and README's Usage section (README.md:53-58) shows them called without the now-required `user_home` argument — broken as written. Open branches: (a) deliberate seed of the future EPUB path — fix README signatures and mark pre-release; (b) unreaped growth — delete API and README section. No user ruling exists; either way the README sample must change. Do not delete or spec this API without the author's pick.
- **B-62 — Story-status split-brain.** `validate-filter-values.yaml` and `filter-by-sample-flag.yaml` read `status: done` and sit in TODO.md's Completed Features, while TODO.md's In Progress section lists the same stories (plus `filter-by-reading-status.yaml`) as needing proper ATDD reimplementation after the 6f786cc lapse. By the project's own principle — a feature without process provenance is not done — the needing-reimplementation side wins, but the remedy is contested and the author's: (a) mark cards `reopened` in place, keep the code; or (b) merge/replay the unmerged PR #18 semantic revert and mark them `backlog`. Underlying cause: dual bookkeeping between TODO.md and story cards. Practical rule now: never trust a declared status — verify against implementation and `git log` before acting.
- **B-63 — The unspecified borrow.** `cli.py` imports `SUPPORTED_FILTERS` from the library (cli.py:10) — the CLI-borrows-library-vocabulary contract — but no spec asserts the identity; `specs/apple_books/library_spec.py:21` asserts set equality only, so `cli.py` could grow its own matching literal and nothing would fail. Proposed one-line spec exists (`assert cli.SUPPORTED_FILTERS is library.SUPPORTED_FILTERS`, author found the syntax reasonable) — fold into the first repair story, do not add it outside one.
- **B-64 — Known doc rot is ledgered, not license to fix.** README advertises unbuilt features (TOC/highlights/export); `docs/pre-commit-workflow.md` documents the dead black/flake8 toolchain; CLAUDE.md carries revert-collateral wording (commit-cycle phrasing, the stale AGENTS/GEMINI duplication note, unqualified type-hints and all-tests-passing lines). All of it is captured with proposed remedies in `.coordination/verified-residue.md` and the compile-phase contradiction ledger, awaiting a gardening pass the author must green-light. Do not repair docs or CLAUDE.md ad hoc.

## The 2026 reevaluation layer

- **B-65 — What changed in 2026-08 and where the meta-project lives.** Restorations executed at the author's direction: spec tree reverted wholesale to the pre-reorg concern-based checkpoint (53 tests green); commit-after-RED restored to CLAUDE.md; AGENTS.md/GEMINI.md converted to symlinks to CLAUDE.md (the sync question is structurally dead). `.coordination/` is the committed substrate of the reevaluation: mined rules corpus (`mining/v2/`), compile policy and eval design (`eval-design.md`), the contradiction ledger with author ratifications (`compile/contradiction-ledger.md`), repair backlog (`verified-residue.md`), and tools (`tools/validate_mining.py` — known blind spot: it verifies `quote:` fields only, quoted text elsewhere is unaudited). Graduation of `.coordination/` content into docs/ or stories is deferred to the author. Nothing produced there is repo rule until he ratifies it wholesale. The repo's stop hook encodes a single-agent invariant that misfires in multi-agent sessions; the author ordered it disabled — do not resurrect it.
- **B-66 — This skill is the project's memory.** Cloud environments have no cross-session memory; the git repo is the only cross-session substrate, and this skill is the explicit, loadable encoding of what the project is — the compiled successor of the 2025 session diary, which was the same cure for the same amnesia. When durable project knowledge lands (a ruling, a schema verification, a settled decision), it belongs here or in the artifacts B-60 points at — knowledge that exists only in a conversation is already lost. Keep this skill's claims verified or stamped: a fossilized memory is worse than none.
