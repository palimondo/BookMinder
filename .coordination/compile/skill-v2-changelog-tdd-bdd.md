# tdd-bdd skill v2 changelog — judge pass over transcript-level provenance verification

Inputs: provenance-verification-tdd-bdd-{1,2,3,4}.md (every corpus source behind T-01..T-99 re-read against primary transcripts). Every change proposal was adjudicated; load-bearing ones were re-verified against the transcript before applying (T-99/T-81 window, T-10's two sources, T-53's retraction, T-56's demotion, T-88's disjunction, T-90's amend habit — all confirmed at the cited locs). Verdict base rates across the four reports: 164 FAITHFUL corpus rows, 6 DISTORTED, 15 UNDERREACH, 5 OVERREACH. Result: 23 proposals accepted (5 of them in modified form), 1 systemic instruction applied in modified form (reasons below), 0 proposals rejected outright; 19 rules reworded, 1 rule added (T-100), 0 rules removed — no rule was left with zero faithful sources.

## Rule text changes (skill pages)

**T-09 (outside-in.md) — added the disclosure-is-not-a-licence clause.** Old: state explicitly when your evidence set cannot falsify the hypothesis. New: adds "a stated limitation is a report, not a licence... it is the signal to go query the real source." Why: report 1's UNDERREACH on d011-R8 — the agent stated the fixture's blind spot precisely, twice (day-011:L7122-L7125, L7160), offered documented-with-caveats plus a backlog item, and was refused ("No, you need to perform the research on my read databases, not on the fixtures!", L7172); the real library then disproved the assumption (L7653). As compiled, the second clause read as the cheaper of two parallel options.

**T-10 (cycle-and-commits.md) — dropped one clause, restored one qualifier.** Old: "re-run it after every edit to the spec itself." Dropped: its only support was d017-R14, whose loc (day-017:L1556) is the agent's own pytest invocation with no author turn within fifty lines; the quoted "Focus on TDD discipline" opens the rename-scope turn at L1608, already compiled as T-22 — and the same window contradicts the clause (spec edited at L1526 and L1543, run once at L1556). Verified directly. Restored from d010-R1: "even when the defect was just observed in a real run on a real machine, because real-world feedback is not a licence to bypass RED" — the qualifier that is the whole day-010 episode ("I wouldn't celebrate! This was a `<tdd_discipline>` violation", L2881; "Revert." L2908; verified directly). Report 1, DISTORTED(misattribution) + UNDERREACH.

**T-17 (cycle-and-commits.md) — added the staged-diff check at the GREEN commit.** New tail: "Commit only what the red spec drove: read the staged diff and back out any production change no spec demanded before the commit lands." Why: report 1's UNDERREACH on d016-R9 — between the commit ask (day-016:L6889) and the commit, the author read the staged diff and made the agent revert an unspecified ZSTATE=6 edit out of it (L7169-L7190). T-98 governs the message against the diff and T-15 governs how much you implement; nothing governed the tree at the cycle's commit boundary.

**T-20 (cycle-and-commits.md) — named the unit of one refactoring.** New middle clause: "one refactoring means all of it: apply the change to every site it covers in that commit, never one call site per commit — the trickle is as unreviewable as the mixture." Why: report 1 — two turns after the mined quote the author rejected site-by-site commits ("nah, we don't need to inline one by one - inine all, and commit", day-020:L1862, recurring L24347). The failure mode is symmetric and nothing else covered the trickle direction.

**T-100 (cycle-and-commits.md) — NEW RULE, from d006-R3 relocated out of T-29.** Text: never claim the discipline produced an improvement until at least one complete cycle has been executed and observed, and never compare stages that are not comparable — a walking skeleton against a feature-complete predecessor measures scope, not method. Why: report 2's UNDERREACH — d006-R3's episode (author demolishing "44% code reduction", "0%→85% coverage", "we didn't even get to the refactoring stage of the TDD's inner loop", day-006:L1793-L1797) teaches a different obligation, in a different tense, than T-29's execute-before-claiming-ready; nothing in the skill forbade claiming method efficacy. Chose the verifier's option (b) — standalone rule here — over (a) rerouting to the pairing skill, because the claim is about this discipline and belongs on the page that owns its cycle. Index row added; disposition d006-R3 merged→compiled.

**T-45 (writing-a-spec.md) — restored the toolchain half.** Old: don't annotate spec functions. New adds: "when the type checker demands them, exclude the spec tree from type checking rather than annotating specs to satisfy it, while leaving lint and format applied to specs." Why: report 2's UNDERREACH on d005-R12 — the remedy that landed was the mypy exclude (day-005:L1573-L1583) and only then the annotation revert (L1591-L1600); the compiled rule told an agent what not to write but not what the author did about the gate. Also makes the CLAUDE.md type-hints tension explicit, as ledger L-16 already resolves it.

**T-50 (assertion-integrity.md) — negatives as absence, and one unattested superlative cut.** Old: "fewest fields, fewest assertions, shortest expression... do not enumerate negative cases alongside it." New: drops "shortest expression" (report 2: attested by neither source window) and replaces the bare prohibition with "express the negative as absence of the property under a non-emptiness guard (T-59)." Why: the episode did not delete the negative case — the author re-specified it twice, stronger ("absence of cloud, without tying us to any specific book", day-018:L585; "output length is bigger than 0 and doesn't contain any clouds", L592). As written, T-50 read as licence to delete the negative check.

**T-53 (assertion-integrity.md) — FIXME mechanism replaced with name-the-owning-layer.** Old tail: "mark FIXME naming the right technique and owning layer, and move on." New: "say which layer owns it and leave the spec asserting only what this layer can falsify — a limit belongs to whichever spec controls the volume, not to a count copied out of the fixture." Why: report 3's OVERREACH, verified directly — the FIXME was a mid-thread suggestion the same exchange retracted ("nah, I see now... it will not be tested with moch, but only that we are passing the upper size limit into the query parameter", day-020:L2605); what landed was `assert len(books) > 0` with no marker. The new wording is also what the ratified ledger L-01(b) resolution actually says (limit properties at the layer that controls volume); the FIXME clause came from the pre-dissolution draft of L-01 and survived compilation by accident.

**T-54 (assertion-integrity.md) — fix-the-fixture points at T-76.** Old: "fix the fixture to contain it." New: "...by copying the real record in (T-76), never by authoring one." Why: report 3 — in the same exchange the author interrupted the agent's hand-authored INSERT ("Can't we develop a script … that copies the complete entry between the real database and the test fixture?", day-015:L2200-L2201). The rule sent the reader to the fixture and stopped exactly where the author had to intervene.

**T-56 (assertion-integrity.md) — recover-why-it-exists made the first step.** Old: enumerate the properties the spec constrains, show the replacement constrains each. New prepends: "recover why it exists — trace the construct's history (git log -S) and read the change that introduced it" and closes with "a construct whose motivation you have not recovered is not yours to replace." Why: report 3's UNDERREACH, verified directly — the mined half was the half the author demoted ("But all that's beside the point. We have introduced the mock for a reason. Check history...", day-020:L56044-L56046); his emphasized instruction was absent from the rule.

**T-59 (assertion-integrity.md) — mechanism corrected, sweep added, T-76 cross-referenced.** Old third clause: "an assertion invented from memory can name data that was never there." New: "an assertion carried over from an older fixture, or copied from a neighbouring spec, names data the fixture no longer contains; when you find one, grep the tree and fix the story cards and docs that carry the same stale datum." Why: report 3's DISTORTED(misattribution) on d018-R3 — "The Pragmatic Programmer" was in the fixture on day-015 (L1037) and replaced on day-016 (L2642); the agent traced it to the existing spec and story card (day-018:L526-L534), and the author asked separately for the stories sweep (L540). Also "fix the fixture first" now reads "by copying from the real source (T-76)" — same gap as T-54, second session (day-018:L1308 "By Hand? We have script for that?").

**T-61 (assertion-integrity.md) — source dropped, boundary against T-70 added.** Removed d020s5-R5 from the rule's support (report 3 DISTORTED: the cited line sits inside a diff the author rejected for non-equivalence and unrecovered motivation, day-020:L55995-L56046 — no turn concerns message coupling); rule survives on d011-R10. Added: "assert the sentinel your own double injected, never the real wording or path the layer below produces — verifying pass-through (T-70) and coupling to a lower layer's internals are different claims." Why: without it, T-61 (no full internal message text) and T-70 (verify the messages pass through) read as a conflict; the sentinel distinction is what the day-020 spec that landed actually does (asserts the mock's injected "Permission denied", L15184).

**T-62 (assertion-integrity.md) — equal-strength case added.** New: "when two specs constrain the same behavior with equal strength, keep the one whose motivation you can state." Why: report 3's UNDERREACH on d020s3-R7 — "3 and 4 is the same and motivation for keeping it is 4, so dump 3" (day-020:L14591) is not a subsumption call; T-62's stated mechanism (whole-output equality subsumes) could not decide that case, and s3-R7 is the only one of T-62's four sources that is a distinct episode.

**T-76 (layers-and-seams.md) — announce fixture directories.** New tail: "Announce any directory you add under the fixture tree in the turn you add it — an empty directory is invisible to `git status`, so an unannounced speculative fixture tree cannot be audited." Why: report 4 — the dropped operational half of the same author turn ("my inability to see using git which directories in `fixtures` were added by you in this speculative manner... I was expecting to see them as untracked files, but I got nothing", day-020:L4634), the problem T-79's leaf `.gitkeep` later resolves from the other end.

**T-83 (layers-and-seams.md) — coverage across the move.** New tail: "Check coverage across the move: a drop after the replacements land is evidence they exercise a different path than the specs they replaced, never an accepted cost of the restructuring — find the line and explain it before committing the deletion." Why: report 4 — the day-020 arc followed T-83's land-then-delete sequence and still broke: coverage dropped 97%→94%, the agent explained it away as expected (L2796), and the author had to catch the relative-vs-absolute path regression two sessions later (L4419, fix L4522-L4524); the day-021 source shows the wanted behavior (trace the dropped lines before deleting, L2928-L2947). Without the tail, T-83 licensed the exact sequence that produced the regression.

**T-88 (minimal-implementation.md) — delete-only became the author's disjunction.** Old: "...then delete the code — never write a test whose stated purpose is to execute a line the coverage report flagged." New: "take one of the two exits the investigation opens — delete the line if it is defensive residue no requirement demanded, or write the missing spec if it names a real behavior nobody specified. The forbidden third option is a test whose stated purpose is to execute the flagged line." Why: report 4's double UNDERREACH, verified directly — both author-sourced episodes are disjunctions: "then the proper fix would be to just remove it. Oralternatively add test for it, if usefull..." (day-020:L55186-L55187), and the day-007 three-way split where `return None` on a missing book was kept and specified with the author's "abosolutelt, this is the way!" (L1854-L1882). As written, T-88 forbade a move the author blessed in the session it was mined from. The page intro's "the fix is removal, not a retroactive test" was aligned to match ("removing the code or specifying the behavior, never a test written to execute the line").

**T-90 (minimal-implementation.md) — commit-message clause dropped.** Old tail: "...watch it fail, revert, and record the demonstration in the commit message." New ends at "revert." Why: report 4's OVERREACH, verified directly — the clause appears in no author turn; it is the agent's own amend habit (`git commit --amend --no-edit` then a self-initiated re-amend, day-021:L4348-L4362), and the message body is never shown in the transcript. Index also notes T-90's two entries are one author turn split at a sentence boundary.

**T-91 (minimal-implementation.md) — skip-as-stand-in clause added.** New: "A skipped spec may hold a place for a scenario not yet started, never stand in for a behavior already implemented — a skip committed next to shipped production code is an unspecified requirement wearing a test's name." Why: report 4's UNDERREACH — the cited d010-R3 skip was never corrected in-session, while the session's actual enforced episode (skip-bodied `it_handles_permission_denied_with_helpful_message` committed beside untested production error handling; "❌ Skipped test instead of making it work", day-010:L2889; "Revert." L2908) was uncited and its shape uncovered: T-04 sanctions skipped placeholders and nothing distinguished a placeholder from a stand-in. Index re-anchored to both episodes.

**T-95 (minimal-implementation.md) — the gate before relaxing the type.** New: "Before relaxing the type, trace why the field was required — git blame the declaration and find its current consumers (T-23) — because the repetition is evidence about the design, not yet a verdict on it." Why: report 4's UNDERREACH — the author gated the type change in the same exchange ("we shouldn't change it wouthout knowing the motivation. We should maybe use git blame...", day-020:L23689-L23690); as compiled, T-95 turned a diagnostic signal straight into a production-type edit, which is the move the author interrupted.

**T-99 (minimal-implementation.md) — CONTRADICTION with T-81 resolved.** Old: "Introduce a shared test fixture or helper only when at least two existing specs consume it in the same commit..." — which forbids the `row_stub` helper T-81 mandates: verified directly at day-019:L4869-L4928, the author himself directed `row_stub(...)` into existence ("OK, I'd call that `row_stub(...)`...") with all four call sites inside one newly written `it_` — zero existing consumers. New: scope narrowed to "a shared pytest fixture or cross-spec helper" (T-99's own episode, the `runner` fixture justified by anticipated conversions, day-021:L2432 — verified), with "anticipated future conversions are not call sites" added from that episode's substance, and an explicit exemption: "A data builder landed together with the specs that consume it is not a shared helper and not speculative — the stub-helper pattern (T-81) governs it." The two rules now partition the space instead of overlapping on it.

## Index-only and disposition-only changes

- **T-40**: d012-R5 (day-012:L1338) added as second source — T-40's "under a skip marker" contradicted its only citation (the author's "body is just `pass` nothing else", L1074); the skip half arrives at L1338. Rule text unchanged (report 2's judgment that the synthesis is right, only under-sourced, adopted).
- **T-37**: text kept, provenance corrected. The interleaving clause is user-verbatim (day-007:L2712-L2713); the depth cap is agent-synthesis from the accepted restructure (L2757-L2807) ratified only in the unratified canonical §2.1 — the index now says so instead of presenting the cap as user-verbatim. Chose re-provenance over softening the text because the cap is entailed by one-describe-per-function plus the it_ level, and the restructure that embodies it was accepted without objection.
- **T-64**: loc extended to the correction turns (day-010:L5866, L5911-L5912) — the cited question was answered by the agent committing the anti-pattern; the teaching is two turns later. Rule text already matched the correction.
- **T-08**: d015-R13 loc corrected — quote lives at day-015:L787-L788, not the indexed L691 (a weaker precursor turn on the same thread).
- **T-72**: d009-R11 caveat loc corrected — quote at L5191, not L5037.
- **T-12, T-57**: provenance relabelled agent-synthesis (both rest on agent self-narration/diagnosis with no author turn supplying the rule; the author supplied dissatisfaction, not the wording).
- **T-89**: d011-R6 relabelled — its "user-verbatim" quote is a TODO.md bullet written by the author in day-009, occurring in day-011 only inside diffs, with no author turn in the retrofit window (L2216-L2857). Rule keeps three sources; one is agent-only with an imported verdict and is marked so.
- **T-47**: d011-R7's embellished because annotated (the corrupted-DB scenario survived the challenge; only one of three proposed scenarios collapsed under it, and one had already been self-struck).
- **T-18**: d019-R6's "three times" because annotated as unsupported at loc (author names one instance); compiled text never carried the count, so no text change.
- **because_source relabels** (synthesized because under a user-verbatim label, per verifier flags): T-01, T-04 (d012-R5, d018-R1), T-25, T-26, T-33, T-36 (mechanism-noun enumeration), T-42, T-64, T-79 — annotated "(because: agent-synthesis)" in the provenance column.
- **d017-R14 disposition**: refiled T-10 → T-22 (the quote belongs to the rename-scope turn T-22 already carries).
- **d020s5-R5 disposition**: merged-duplicate → rejected (retracted on verification).
- **d006-R3 disposition**: merged-duplicate under T-29 → compiled as T-100.
- **d010-R2 / d012-R5 dispositions**: annotated with their new secondary roles (T-91 clause / T-40 source).

## Systemic corrections

**(a) day-020 five-fold re-rendering deduplicated.** day-020.md contains five renderings of one conversation (banners at L3/L7207/L21811/L31130/L45521); mining shards s1-s5 cut it into contiguous ranges, so single author turns were mined up to three times as apparently independent corroboration. Every affected index row now lists one entry per episode with "same turn re-exported" marking the duplicate locs. Deduplicated support counts: T-19 3→1, T-20 2→1, T-23 2→1, T-32 3→2, T-36 4→3, T-43 3→1, T-53 2→1, T-60 5→2, T-62 4→2, T-70 3→1, T-71 4→2, T-76 day-020 3→1 (3 episodes total), T-83 day-020 3→2 (3 total), T-84 3→2. Repetition in these rows must no longer be read as reinforcement; T-43, T-70 and T-90 in particular are taught-once.

**(b) flagged index rows fixed.** Wrong locs (T-08, T-72), misattribution (d017-R14 out of T-10), agent-loc annotations (T-39's d012-R4 is a rendered --spec line; T-76's d010-R7 and T-91's d010-R3 are agent-only), and the because_source relabels listed above.

**(c) retracted sources.** d020s5-R5 removed from T-61 — rule survives on d011-R10 (which itself is a code listing no author ever challenged; T-61 stays agent-synthesis and now carries the T-70 boundary that makes it coherent). d017-R14 removed from T-10 — rule survives on d002-R1 + d010-R1, both re-verified. For T-53, the brief's example treated its two OVERREACH sources as retracted; I diverged deliberately: the OVERREACH attached only to the FIXME clause, which is now deleted — the same window enforces the surviving clauses twice (fix-the-assertion blocked at day-020:L2566 and L2573), so the deduplicated episode remains as support for what the rule now says, alongside ledger L-01(b). Removing it would have detached a rule from an episode that genuinely teaches it. **No rule ended with zero faithful sources; no rule was removed.**

## Rejected proposals

None rejected outright. Five accepted in modified form, where the modification is itself an adjudication: T-09 (portable wording without transcript citations — skill form rules), T-29's d006-R3 (option b, new T-100, over option a rerouting), T-37 (re-provenance instead of text softening), T-76's new clause (reworded portable), and the T-53 source-retraction instruction (kept the deduplicated episode; reasons in (c)). One verifier alternative was declined inside an accepted proposal: report 1 offered "drop or re-source" for T-10's re-run clause — dropped, not re-sourced, because no episode in the corpus teaches re-run-after-spec-edit and inventing a source would repeat the defect being repaired.

Validator: `quick_validate.py .claude/skills/tdd-bdd` — passing after all edits.
