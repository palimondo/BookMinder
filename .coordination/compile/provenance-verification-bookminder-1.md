> NOTE (2026-08-15): line refs in this report cite the pre-repair day-020.md (blob 27a5ad7d51f6de8db0ef3361df7bc9c9adc034a2). Locs ≤31128 are unchanged in the repaired file; locs ≥45520 correspond to repaired line minus offset — subtract 23710. See .coordination/day-020-repair-report.md.

# Provenance verification — bookminder skill, B-01 … B-31

Transcript-level verification of every corpus source behind the compiled facts/rules B-01 … B-10 and B-20 … B-31 of `.claude/skills/bookminder/`. B-32 is excluded by construction (post-compile author directive, no mining provenance). Each corpus entry was resolved with `.coordination/tools/rule.py` and judged against the primary transcript window at its cited loc (~±55 lines, wider where the entry cites a chain), on four axes: quote exactness, episode fidelity, generalization fit, missed context in the window.

Non-corpus sources — `threads.md` rulings, ratified ledger entries, `repo-map.md`, `verified-residue.md`, `skill-removals-for-project-skills.md`, CLAUDE.md/TODO.md lines and `verified@tree` stamps — are out of scope for this pass and carried as OUT OF SCOPE rows so the coverage gap is visible. Six rules in range (B-02, B-03, B-04, B-09, B-10, and the tree half of several others) rest on nothing else, so nothing in them was verified here.

One tooling note for whoever runs the next slice: `rule.py`'s `LOC_REF` only matches locs that repeat the `day-NNN:` prefix, so the second and later locs in a chained `evidence:` field (`day-019:L2001, L2026-L2044, L2908`) print no window at all. Several entries in this range carry their real evidence in those uncited-by-the-tool positions, and they were fetched by hand. No id in this range collides between `mining/v2/` and `mining/pairing/`: every collision in the corpus is a `p`-prefixed id, and this range cites only `d`-prefixed ones. Philosophy entries (`B-01`, `B-05`, `B-07`, `B-24`, `B-29`) are not rule ids and do not resolve through `-r`; they were located in the day-file YAML and their windows printed directly.

"Wrap" in the quote column means the mined string is character-exact but the transcript renders it across a wrapped line pair.

| rule | source | quote | episode | generalization | missed context | verdict |
|------|--------|-------|---------|----------------|----------------|---------|
| B-01 | v2/day-002 phil @ day-002:L94 | exact, prefix of the turn | matches: requirements dialogue, author's four-point answer | follows for the destination and the iPhone/last-chapter/highlights triple | the CLI+MCP walking-skeleton half of B-01 is at day-002:L113 ("Command line tool and MCP integration. So an MVP of broader vision."), a loc the entry does not cite; "knows CLI cold, MCP unfamiliar" is threads.md only | FAITHFUL |
| B-01 | threads.md:21 | — | — | — | — | OUT OF SCOPE |
| B-02 | threads.md:22, :26, :28 | — | — | — | — | OUT OF SCOPE |
| B-03 | threads.md:24 | — | — | — | — | OUT OF SCOPE |
| B-04 | threads.md:25 | — | — | — | — | OUT OF SCOPE |
| B-05 | v2/day-018 phil @ day-018:L5021 | exact (wrap L5021-L5022) | matches: agent's "vision drift / process over product / cruft" audit, author's correction | follows | L5048-L5060, the very next turn: CLAUDE.md rules are "definitely in WIP", "many techniques employed in them are untested", and "only docs/apple_books.md is really solid and much of the rest ... is basically just AI slop" — the vehicle claim arrives already qualified | UNDERREACH |
| B-05 | v2/day-009 phil @ day-009:L8039-L8041 | exact (wrap), author's own ellipsis preserved | matches: benchmark sketch floated as a sidequest | follows | the same turn opens with the slop warning at L8038-L8039 ("It might be just turn out ot be just a big  baggage of AI slop in the end") — benchmark aspiration and slop warning are one breath | FAITHFUL |
| B-05 | v2/day-009 phil @ day-009:L8222-L8223 | exact (wrap) | matches: Patton/story-mapping turn, PO day job, story cards expanded into acceptance tests | follows | none | FAITHFUL |
| B-05 | v2/day-013 phil @ day-013:L1446 | exact (wrap L1446-L1447), typos preserved | matches: meta-project framing before a commit-attribution check | follows | the turn's operative half is a distrust check on Gemini's commit attribution; the meta-project sentence is preamble | FAITHFUL |
| B-06 | d006-R6 | exact @ L3278, typo `ORIGINAL_VISON.ms` preserved | matches: `Read(../BookMind/ORIGINAL_VISION.md)` → `Error: File does not exist.` @ L3034-L3036, then reasoning from memory | follows; the `git show 64dc71b:README.md` verification is the agent's own, executed and confirmed @ L3297-L3300 | none | FAITHFUL |
| B-06 | d006-R7 | exact @ L1793-L1794 (wrap), typos preserved | matches: BS commit message @ L1740-L1770, author's correction | follows; "EPUB parsing … 40 passing tests" is corroborated later in the same session (L3319 `40 passed`, L3322-L3328 the CLI) rather than at the cited loc | L1839-L1842: the author reframes the whole exercise as qualitative, not quantitative — strengthens the rule | FAITHFUL |
| B-06 | d006-R8 | exact @ L1794-L1795 (wrap) | matches | follows | same turn as d006-R7; one author turn, two entries | FAITHFUL |
| B-07 | d005-R8 | exact @ L2229-L2230 (wrap) | partly: the author says he "explicitly stopped you from running ahead with implementing the project with your own creative ideas as happened in BookMind"; the `because` upgrades that to "The predecessor project BookMind **failed**" | rule follows; the compiled B-07 does not inherit the distortion | day-006:L1793 has the same author saying BookMind "did a ton of things", and B-06 forbids calling it a failure — the corpus `because` contradicts a sibling compiled rule | DISTORTED (embellished-because) |
| B-07 | v2/day-006 phil @ day-006:L13 | exact | matches: controllability/sprawl/credit-cost framing | follows | none | FAITHFUL |
| B-07 | v2/day-005 phil @ day-005:L2228 | exact (wrap L2228-L2230) | matches | follows | L2319-L2322: the author rejects the resulting commit for dropping the CI-mock and GH-workflow tasks — unrelated to scope discipline | FAITHFUL |
| B-08 | d007-R15 | exact @ L7759-L7760 (wrap) | matches: commit message claimed the macOS dependency was removed; author objected | follows | `runs-on: macos-latest` verified verbatim @ day-007:L5990 | FAITHFUL |
| B-09 | threads.md:31-36, :73, :77; bdd-style artifacts; TODO.md:19-20 | — | — | — | — | OUT OF SCOPE |
| B-10 | threads.md:33, :117; skill-removals persistence fragment | — | — | — | — | OUT OF SCOPE |
| B-20 | d008-R19 | exact @ L2702 | matches the *event* (a bash `find` right after `/hi`) but not the *complaint*: the author asks why `find` was run at all when `/hi` had already run `tree`; nothing in the turn is about `.venv` noise. The "three project files and seventeen site-packages paths" is inferred from a collapsed `… +17 lines` line | overreaches its own quote: the rule is about filtering noise, the episode taught reusing the session-start census | L2716-L2719: seconds later the author interrupts again ("you are rushing ahead… I'm switching you to plan mode") — the turn is one beat of a re-discovery/rush pattern, not a tooling tip | DISTORTED (embellished-because) |
| B-20 | repo-map.md; skill-removals claims fragment; HEAD line count | — | — | — | — | OUT OF SCOPE |
| B-21 | d002-R10 | exact @ L889 | matches: the agent had been writing into `claude-dev-log-diary/day-002.md` (diff visible L849-L882); author stopped it | follows | the same turn continues "By updating project memory I meant you should have modified the `CLAUDE.md` file" (L890-L891) — the entry drops the second half, which is where "project memory = CLAUDE.md" originates | FAITHFUL |
| B-21 | d017-R1 | exact @ L285 (turn opens L284) | matches: author restates the diary rule with its reason | follows | the git-mediated leak the rule names is real and in this session, but at uncited locs: `git diff --cached` @ day-017:L125-L126 and `git diff HEAD` @ L152-L153 both paginate `claude-dev-log-diary/day-016.md` | FAITHFUL |
| B-21 | d020s5-R7 | exact @ L56097 | matches: a background `Task` running `grep -n -i -C 10 … day-019.md \| head -200` was killed on sight | the attested distinction is *background subagent*, not sweep breadth — the rule's "never a broad `grep … \| head -200` sweep" does not follow | L56101-L56116: immediately after the kill the agent runs `rg -g "day-018.md" -B20 -A20 … \| head -200` and three more diary greps in the foreground, all unchallenged | OVERREACH |
| B-21 | d014-R9 | exact @ L340-L341 (wrap), typo `persmisson` preserved | matches: permission granted for one named range of one named file | follows | L417-L419 is the enforcement half and the source of the `because`'s cost figure: "you just wasted 10% of your context on ingesting complete logs, when I did specifically give you line numbers to read" | FAITHFUL |
| B-21 | d017-R7 | exact @ L2304-L2305 (wrap) | matches: agent proposed `.gitignore`-ing `All_Books*.swift`; author rejected | follows | the same turn commissions the PreToolUse hook blocking `git add .`/`-A` (L2305-L2306) — the origin of CLAUDE.md's explicit-staging rule | FAITHFUL |
| B-21 | d006-R9 | exact @ L227-L228 (wrap), unclosed paren preserved | matches: author corrects cost-accounting method and names the `^claude` session boundary | follows | none | FAITHFUL |
| B-22 | d020s2-R13 | exact @ L7564 | matches: agent explained the coverage gap as untested edge cases; author corrected; agent found the subprocess cause (L7845-L7846, L7860) | follows | none | FAITHFUL |
| B-22 | git ls-files at HEAD; threads.md:77 | — | — | — | — | OUT OF SCOPE |
| B-23 | d011-R16 | exact @ L11281 | matches: Gemini proposed `status` + `priority` + `assigned_to`; author kept only `status` | the six-value vocabulary in the rule is the *agent's* proposal list (L11257-L11262), not the author's, yet the entry carries `because_source: user-verbatim`; the author endorsed the field, not the enumeration | same turn: "We shoudl probably also document this whole business with stories in our agent directions" — the origin of CLAUDE.md `<backlog_management>`, which is where the compile correctly re-anchored the vocabulary | DISTORTED (misattribution) |
| B-23 | d011-R17 | exact @ L9096-L9098 (wrap), unbalanced paren preserved | matches: agent planned `stories/admin/`, `stories/error_handling/`, `stories/research/` (L9062, L9067, L9072); author put all of it in `discover` | follows | L9112: the agent then ran `mkdir -p stories/discover stories/admin stories/error_handling stories/research` anyway — the correction had to land twice | FAITHFUL |
| B-23 | d017-R10 | exact @ L2020 | matches: agent had added ➡️ emoji, numbering and per-item rationale to the Discover Column | follows | L2026-L2028 supplies the `because` ("the correct implementation order from top to bottom -- there's no need for describing it further") and the story-name rhythm rationale | FAITHFUL |
| B-23 | d017-R11 | exact, spans L2020-L2021 (entry cites the closing line) | matches: "**Filter System Refactoring:** Renamed --flag to --filter" added to Completed Features @ L1992 | follows | none | FAITHFUL |
| B-23 | d017-R15 | exact @ L2570-L2571 (wrap) | matches: parking the hooks task for a future session | follows | the parked item as written (L2579-L2581) contains mechanism, trigger, blocked patterns and expected message — the rule's enumeration is descriptive of this artifact | FAITHFUL |
| B-23 | d018-R11 | exact @ L1112 | matches: agent listed five stories and asked "Which would you prefer to work on?" | follows | L1125: the author's next move is to send the pair back to a skipped refactor phase — order discipline and cycle discipline in one turn | FAITHFUL |
| B-23 | d018-R20 | exact @ L5502 | matches: 12 docs deleted in commit eea59e3, author asks where the deletion should be recorded | follows | the cited loc is the *question*; the form the rule states (one line, in Completed Features) is settled at L5530 ("compress it into single point and move it into the section where we keep note of completed stories") and executed at L5537 — locs the entry does not cite | FAITHFUL |
| B-23 | d021-R15 | exact @ L8745-L8746 (wrap) | matches: agent moved the entry out of Completed Features into a new In Progress section; author asked for in-place notation; `[REOPENED: needs to include samples]` resulted @ L8756 | follows | none | FAITHFUL |
| B-23 | CLAUDE.md `<backlog_management>` at HEAD | — | — | — | — | OUT OF SCOPE |
| B-24 | d020s2-R14 | no quote field (agent-synthesis, honestly labeled) | matches: `TypeError: 'Group' object is not subscriptable` @ L9212, rename @ L9489 | follows | this exchange is duplicated verbatim inside day-020.md — see d020s4-R17 | FAITHFUL |
| B-24 | d020s4-R17 | no quote field (agent-synthesis) | matches @ L23197 | follows; UP035/UP006 are the agent's rule codes, the transcript says only "Ruff wants us to use the modern list type annotation" | L23167-L23227 is byte-for-byte the same edit sequence as L9459-L9519 (only the path rendering differs), so d020s2-R14 and d020s4-R17 are one episode counted twice | FAITHFUL |
| B-24 | d019-R21 | exact @ L2908-L2909 (wrap) | matches: author proposed the `with_` name; agent adopted it | follows | the decorator exists because the author refused the duplication at L2000-L2001; "Click does not inherit group options" is the agent's assertion (L1992-L1993), never demonstrated | FAITHFUL |
| B-24 | d017-R8 | no quote field; evidence is the approved plan @ L1294 | matches: plan approved at L1294 after the author's steer at L1288-L1290 | the mined rule's load-bearing negative ("there is no `--flag`, `--status`, or `--type`") is dropped from the compiled B-24, and the approved plan's reading-status values (`--filter finished` / `unread` / `in-progress`, L1305-L1308) are absent from the compiled four-value enumeration | L1282-L1285: the *rejected* plan variant is the one that kept `local`/`non-sample` as separate values and deferred multiple filters | UNDERREACH |
| B-24 | d017-R9 | exact @ L778-L779 (wrap) | matches: author questioned `-cloud` against Click's parser and offered `!` | follows | the Click-parsing claim was answered by assertion (L798), never demonstrated; the author had asked "or am I wrong?" — B-31's own "untested causal claim" standard applies to it | FAITHFUL |
| B-24 | d021-R20 | exact with marked elision @ L6139-L6140; both fragments exact | matches: filter-validation story scoped filters per command (L6121); author rejected command-specific filters | follows | same turn L6141-L6143 supplies "the negative filter are derived from the positive ones" and defers the both-forms-of-one-filter warning to the multiple-filters story | FAITHFUL |
| B-24 | d021-R19 | exact @ L7231 | matches: `WHERE ZREADINGPROGRESS > 0` excludes samples, so `list recent` diverged from Apple's Continue section | follows for the compiled clause; the rule's "~7 day cutoff / include samples / exclude finished" specifics are the agent's synthesis at L7248-L7261, not the author's turn | the author closed with "or should I approach this differently because I haven't considered some important aspect?" and the agent only agreed | FAITHFUL |
| B-24 | d019-R19 | no quote field (`user-paraphrase`) | matches: agent surfaced the story/CLI mismatch (L1626-L1669) and offered three options; author ruled out option 2 (L1674) and approved the `list all` route at L1773 | follows; the story-card update is verified @ L1786-L1804 | the ratifying author turn (L1773 "I agree. Let's do that. And I want to be approwing all your writes and edits!") is not among the cited locs | FAITHFUL |
| B-24 | v2/day-010 phil @ day-010:L186 | exact (wrap L186-L187) | matches: author names the admin use case while weighing env-var vs `--db-path` vs DI | follows: "a test seam earns its place in production only if it is also a real capability" | the same turn commissions the expert-council framing that produced the seam debate | FAITHFUL |
| B-24 | d020s1-R18 | exact @ L2933 | matches: author moves path resolution out of the CLI into the library and asks for unit specs | follows | identical author turn, verbatim, also at day-020:L26198-L26199 — see d020s4-R12 | FAITHFUL |
| B-24 | d020s4-R12 | exact @ L26198-L26199 (wrap) | same turn as d020s1-R18, including the agent's identical reply (L26216-L26223 vs L2935-L2949) | follows | day-020.md re-logs whole regions at differing offsets, so `s1`/`s2` and `s4` ids are not independent evidence | FAITHFUL |
| B-24 | cli.py re-verified at HEAD | — | — | — | — | OUT OF SCOPE |
| B-25 | d009-R25 | exact @ L1909 | matches: `RecentBook` challenged, `BookWithProgress` created instead (L2011-L2014), collapsed to `progress: NotRequired[float]` @ L2259 | follows | the "asked twice" claim is verified: at L2154 the author switched model and said "Opus, please review what Sonnet did (and what I asked for!)", and Opus's reading @ L2161 is that the second type was created against an explicit request | FAITHFUL |
| B-25 | d007-R21 | exact @ L7877-L7878 (wrap) | matches: author asked whether `TypedDict` gives dot access | follows | the answer was *demonstrated*, not asserted: `book.title fails: 'dict' object has no attribute 'title'` @ L7897 — one of the few empirically settled claims in this range | FAITHFUL |
| B-25 | d020s2-R15 | no quote field | the two cited locs are code archaeology, not an exchange: `path="",  # Path requires Books.plist correlation with ZASSETID` @ L10258 and the `NotRequired` edit @ L10416, introduced by the agent's own "let's make the fields optional as the tests suggested" | rule follows from the code | no author turn appears in either window, yet the entry carries `because_source: user-paraphrase` and the rule index promotes B-25 to "user-verbatim" | DISTORTED (misattribution) |
| B-26 | d010-R17 | no quote field (agent-synthesis, honestly labeled) | matches: the four validated scenarios @ L1272-L1290 | follows | L1261-L1264 is the author pasting `sudo ls -la` output from a second real account (katka) — the primary evidence for every persona claim in this rule family | FAITHFUL |
| B-26 | d010-R21 | exact @ L3627-L3628 (wrap) | matches: author rejected the env-var/DI proposals and supplied the dual reading of `--user` | follows | the turn opens "OK, that was a waste of tokens, Opus. :-(" — the seam is the author's design, arrived at against the agent's | FAITHFUL |
| B-26 | d020s1-R19 | exact fragments with marked elision @ L3317 | matches: three rejected spec edits, then the author's enumeration of the cases | rule follows | the `because`'s "Python has no accidental falsy strings to guard against" inverts the author, who *asked* — "is there a string value that could accidentally be evaluated to false? Or is this a Javascript trauma a Python does't suffer from" — and the answer is that `""` is falsy; also L3325 ("3 and 4 is the same and motivation for keeping it is 4, so dump 3") is the turn that fixed the case list | DISTORTED (embellished-because) |
| B-26 | d020s1-R20 | no quote field | the cited loc is not an author turn: L5445 sits inside a Gemini-authored summary of day-010 that the agent pasted verbatim, yet the entry is labeled `user-paraphrase` | the underlying claim is sound — corroborated at day-010:L1276-L1279 and by the author's own `sudo ls` at day-010:L1261-L1264 | the same summary is re-logged three times in day-020.md (L5445, L18962, L29479) and mined three times | DISTORTED (misattribution) |
| B-26 | d020s2-R16 | exact @ L7990-L7992 (wrap), typos preserved | matches: author explains why `Path.home()` has no end-to-end test and prescribes the layer split | follows; the move into the library is verified @ L13896-L13977 | the same turn carries a full test-architecture directive (acceptance test reduced to "CLI catches the exception", edge cases to integration) that belongs to the tdd-bdd slice | FAITHFUL |
| B-26 | d020s3-R11 | no quote field (agent-synthesis) | the cited loc is the second re-log of the Gemini day-010 summary (L18962) | the summary lists three personas; the rule's fourth clause ("`corrupted_db_user` has a broken sqlite file") is not at the cited loc | primary evidence remains day-010:L1261-L1290; `corrupted_db_user` is evidenced at day-011:L1862 (see d011-R20) | OVERREACH |
| B-26 | d020s4-R10 | no quote field, labeled `user-paraphrase` | the cited loc is the third re-log of the same summary (L29479) | the rule's distinguishing detail — `never_opened_user` has an *empty BKLibrary directory*, which is why it yields "No BKLibrary database found in:" while `legacy_books_user` yields "BKLibrary directory not found" — appears nowhere in the cited window, and the summary says "No databases or plists" | none in-window; the error-message pair is the part worth keeping and is unsourced here | OVERREACH |
| B-26 | d020s3-R12 | no quote field (agent-synthesis) | matches exactly: `User path: /Users/specs/apple_books/fixtures/users/never_opened_user` @ L16615, diagnosed at L16624-L16627 | follows | none | FAITHFUL |
| B-26 | d014-R7 | no quote field (agent-synthesis) | matches: `ReadFile specs/fixtures/All_Books.swift` (wrong path) → `SearchText 'Lao Tzu'` → "No matches found" (L172-L186), then the pivot to mutating `library.py` to print ZSTATE (L199-L204) | follows | the failing test's own command line named the fixture user (`…oks/fixtures/users/test_reader`, L81); the data was in the binary sqlite, which no text search could reach | FAITHFUL |
| B-26 | ledger L-09; library.py + fixture tree at HEAD | — | — | — | — | OUT OF SCOPE |
| B-27 | d015-R4 | fragments exact @ L2461-L2463, but the mined string joins two non-adjacent sentences with `\n` and silently drops "Let's see if the two books you found there before are real subset from my library." | matches: the 20-column hand-made fixture is verified verbatim @ L2144-L2168 | follows; the "108 columns … plus all ten indexes" figure checks out exactly against the real schema dump @ L2512-L2531 and the ten `CREATE INDEX` lines @ L2532-L2541 | none | FAITHFUL |
| B-27 | d015-R5 | no quote field (`user-paraphrase`) | matches: the `.mode insert` one-liner @ L2383-L2409, its failure, and the ATTACH form verified @ L2733-L2734 | follows | L2423 is the author gating documentation on a successful run — the same turn that anchors d015-R9 | FAITHFUL |
| B-27 | d015-R10 | exact @ L3638 | matches: the agent replaced `find ~/…` with a hardcoded `/Users/palimondo/…` @ L3629-L3635 after being told at L2743 to keep the script portable | follows | L3646-L3649: the actual root cause was `~` not expanding inside the script — the author's "debug your script" was the correct instruction and the agent had been patching symptoms | FAITHFUL |
| B-27 | d016-R1 | exact @ L6634 (leading "Nah, " trimmed) | the episode is a commit-message rewrite, not a fixture-building session, but the author's sentence states the principle the rule generalizes | follows, carried by the day-015 arc it summarizes | none | FAITHFUL |
| B-27 | d016-R2 | exact @ L668 | matches: `_common.sh` had a hardcoded `BKLibrary-1-091020131601.sqlite` | follows | L650-L655 shows the `PROJECT_ROOT` + shared-subpath constants this rule family depends on | FAITHFUL |
| B-27 | d016-R3 | exact @ L1480 | matches: a 0-byte `BKLibrary-test-fixture.sqlite` had been created inside the real Apple Books directory | follows | the causal account (L1485-L1498) is the agent's own, and it names the cause as `FIXTURE_DB_PATH` resolving to `/Users/${USERNAME}/…` — the same zsh-`USERNAME` bug d016-R4 and d018-R17 record | FAITHFUL |
| B-27 | d016-R4 | no quote field (agent-synthesis) | matches, three times over: `./create_fixture.sh test_reader` printed "Creating new fixture for user: palimondo" @ L1203 and L1547, and the copy script the same @ L1572 | follows | L1549-L1551 vs L1557: the fixture was created under `users/palimondo/…` while the agent reported success at `users/test_reader/…` — the misreport is part of why this bug survived | FAITHFUL |
| B-27 | d016-R15 | exact @ L1085 (mid-sentence trim, unmarked) | matches: the draft script contained `find "$FIXTURE_DIR" -name "*.sqlite" -delete` (L1067) and `rm -f "$FIXTURE_DB_PATH"` (L1185) | follows | the same turn asks the agent to state the script's purpose against the story backlog — the single-responsibility framing in the `because` | FAITHFUL |
| B-27 | d018-R12 | exact @ L2216-L2218 (wrap), typos preserved | matches: the agent had `UPDATE`d a fixture row to fit its test | follows | L2225-L2233: the agent's follow-up proposes temporarily mutating the author's *real* library and reverting it, which is the escape hatch the entry's `feasibility` note flags | FAITHFUL |
| B-27 | d018-R13 | exact @ L1308 | matches: agent was hand-extracting column names to build an INSERT | follows | L1313-L1316: the script it should have used then failed — with the `USERNAME` bug — which is exactly the "debug the script" branch | FAITHFUL |
| B-27 | d018-R14 | exact @ L1991 (author writes "bookfinder") | matches: verification run @ L1995 with an absolute fixture path | follows | L2001-L2005: that same verification pass exposed the apostrophe/SQL-escaping bug, the second clause of d018-R17 | FAITHFUL |
| B-27 | d018-R15 | exact @ L2098-L2099 (wrap) | matches: agent was about to spec against a throwaway `fixture-test` persona | follows | L2112-L2114: the book was then copied into `test_reader` with the script | FAITHFUL |
| B-27 | d018-R17 | content-exact @ L1869-L1873; the mined string drops the ```zsh fence and re-indents | matches: the author's one-line shell demo settles a diagnosis the agent had been guessing at (L1859-L1865) | follows | the SQL-escaping half of the rule is evidenced at L2001-L2007, a loc the entry does not cite | FAITHFUL |
| B-27 | d007-R19 | exact @ L1238 (leading "this is good, but " trimmed) | matches: fixture plist path being rewritten for CI; author asked for the derivation to be written down | follows | the author says `/docs/fixtures.md`; the file actually written @ L1255 is `docs/test_fixtures.md`, which is the name the compiled rule uses | FAITHFUL |
| B-27 | d020s1-R21 | exact @ L6656 | matches: redundant `.gitkeep` files at every level of the `never_opened_user` tree | rule follows | the `because`'s second half — "the untracked directory that lacked it vanished and kept a test green for the wrong reason for weeks" — is nowhere in the window; the exchange is only about redundancy. L6672 ("I'm tired, and suspisious of sweeping wipe operaions") is the turn that shaped the cleanup | DISTORTED (embellished-because) |
| B-27 | d011-R20 | no quote field (agent-synthesis) | matches: RED run returned "Apple Books not found. Has it been opened on this account?" @ L1842, diagnosed @ L1862-L1868 | follows | this entry is indexed under B-27 but its content is expressed in B-26's `corrupted_db_user` parenthesis; nothing in B-27 uses it | FAITHFUL |
| B-27 | copy_book_to_fixture.sh:8 at HEAD | — | — | — | — | OUT OF SCOPE |
| B-28 | d019-R18 | no quote field, labeled `user-paraphrase` though the evidence is the agent's own sqlite output | matches exactly: the six-row census @ L1509-L1511 and L1521-L1523 reproduces title-for-title | follows for the demoted use the compile made of it | at day-019 the fixture directory still held both `BKLibrary-fixture.sqlite` and the stale `BKLibrary-1-091020131601.sqlite` (L1504-L1506), and the first query hit the stale one (L1499-L1500) — only one file remains at HEAD | FAITHFUL |
| B-28 | d011-R21 | no quote field (agent-synthesis) | matches: two-book census with timestamps @ L11837-L11838 | follows, and is exactly the census the compile marks superseded | the fixture queried here is the pre-day-016 `BKLibrary-1-091020131601.sqlite` — the rot the compile names is visible in the corpus itself | FAITHFUL |
| B-28 | d002-R13 | no quote field (agent-synthesis) | matches: plist lookup for the GOOS epub @ L2005-L2030, `path` ending `401429854.epub` @ L2023 | follows | the "named in ORIGINAL_VISION.md" half rests on the agent's own framing at L2005; it happens to be true (`ORIGINAL_VISION.md:40-41`) | FAITHFUL |
| B-29 | d021-R16 | exact @ L6321-L6322 (leading "W8, " trimmed) | matches: the agent had been "exploring" sample behavior against the fixture (L6301-L6306) | follows | the same turn records a falsified hypothesis (Mac-vs-iPhone platform inconsistency) — the reason exploration must hit the real DB | FAITHFUL |
| B-29 | v2/day-011 phil @ day-011:L7172 | exact | matches: agent proposed to "revise" the ZSTATE/finished definitions from fixture evidence | follows | L7186-L7188: the agent then asked for the absolute path and stated that providing it constitutes explicit read permission | FAITHFUL |
| B-29 | v2/day-021 phil @ day-021:L7068 | exact (wrap L7068-L7069) | matches: the ~7-day recency finding being written into docs | follows | the same turn is the consistency-sweep directive verified in the P-14 pass (day-021:L7069-L7070) | FAITHFUL |
| B-30 | d004-R15 | exact @ L3079 | matches: `requires-python = ">=3.9"` → `"==3.13.*"` @ L3092 | follows | the two-machine rationale in the `because` is corroborated later in the same session (Mac mini vs MacBook Air, L5313) rather than at this loc | FAITHFUL |
| B-30 | d004-R17 | no quote field (`user-paraphrase`) | matches: hatchling considered and dropped @ L457-L466, author's "OK" @ L468 | follows | none | FAITHFUL |
| B-30 | d004-R18 | no quote field | the cited window (L5713-L5730) shows only the hook being added and passing | rule follows | the whole causal chain is at uncited locs: the invalid `per-file-ignores` table produced `TOML parse error at line 71, column 21` (L5365-L5366), first on the MacBook Air and then reproduced on the Mac mini (L5313), and the author's "these checks should have been executed in our pre-commit hooks, so we should have caught this misconfiguration on TOML file earlier" @ L5652 is what commissioned `check-toml` | FAITHFUL |
| B-30 | d006-R5 | no quote field (`user-paraphrase`) | matches: `source venv/bin/activate && python -m pytest --spec` @ L3284 errors on `--spec` in BookMind | follows | the "40 passing tests in 1.29s" that dissolved the "broken environment" finding is at L3319 | FAITHFUL |
| B-30 | d009-R26 | exact @ L8725-L8727 (author pasted his own console) | matches: `uv run bookminder list recent` began re-downloading CPython and was interrupted | follows | the agent's fallback advice (L8733-L8741) was an alias and a full path — the compiled rule keeps only the `source .venv/bin/activate` form | FAITHFUL |
| B-30 | d010-R23 | no quote field (agent-synthesis) | matches: `target-version = "py313"` made every pre-commit run fail with a TOML parse error (L2327-L2337); reverted to py312 @ L2347 | follows | the `because`'s "cheap-subagent style audit recommended py313" is not in the window | FAITHFUL |
| B-30 | d011-R22 | no quote field (agent-synthesis) | matches: `rev: v0.3.3` visible @ L4033 and the agent commenting the hooks out rather than diagnosing (L4035-L4044) | follows | the venv/CI ruff versions (0.11.10, 0.12.1) named in the `because` are not in the window | FAITHFUL |
| B-30 | d012-R10 | no quote field (agent-synthesis) | matches: `pytest --spec` → `bash: pytest: command not found` @ L38-L40, immediately after `/hi` | follows | none | FAITHFUL |
| B-30 | pyproject.toml pins at HEAD | — | — | — | — | OUT OF SCOPE |
| B-31 | d003-R10 | exact @ L2542 | matches: `requirements.txt` shown redundant and removed @ L2568 | follows; the principle sentence is at another cited loc (L1097-L1098, "I do not need to maintain any backwards comparibility … not keep extra files around that only increate the maintenance burden") and `setup.py` is deleted at L1212 | none | FAITHFUL |
| B-31 | d003-R13 | exact @ L1464 | matches: `Copyright (c) 2024 BookMinder Team` written into LICENSE @ L1439 for a 2025 project | follows | the same turn commissions the contributors entry naming Claude's model version — the origin of CLAUDE.md `<project_maintenance>` | FAITHFUL |
| B-31 | d003-R16 | no quote field (agent-synthesis) | matches: the agent asserted the `__init__.py` necessity twice (L147, L155), created the files, and the very next run still collected 0 tests (L174-L180) | follows, and the episode is stronger than the entry claims | L884-L886: the author flags the contradiction himself ("You said they were optional... but it seemed like they were necessary in our case"), and L942-L943 shows the untested claim written into `docs/modern_python_practices.md` as fact | FAITHFUL |
| B-31 | d015-R9 | exact @ L2423 | matches: the agent announced it would document the one-liner (L2414-L2420) before running it | follows | the one-liner then failed (`day-015:L2444`), so the gate paid for itself in-session | FAITHFUL |
| B-31 | d018-R21 | exact @ L5056-L5057 (wrap) | matches: docs-sprawl review, "only docs/apple_books.md is really solid" | follows | the preceding paragraph (L5048-L5054) qualifies CLAUDE.md itself as WIP and untested, and names the model-switch problem — this is the same turn that qualifies B-05 | FAITHFUL |
| B-31 | d005-R14 | exact @ L607-L608 (wrap, leading "Good, but " trimmed) | matches: README bash blocks with inline comments | follows | none | FAITHFUL |
| B-31 | d006-R4 | no quote field (agent-synthesis) | matches: `find . -name "*.py" -exec wc -l {} + \| tail -1` → `531358 total` / `294940 total` @ L1109-L1116 | follows | this entry is indexed under B-31, whose compiled text says nothing about line counts; its content is what B-20's "never run … repo-wide line counts" is actually made of, and the numbers it produced are the "44% code reduction" claim the author called BS at day-006:L1793 | FAITHFUL |

Counts over 88 corpus source rows: FAITHFUL 76, DISTORTED 7, OVERREACH 3, UNDERREACH 2. Thirteen further rows are OUT OF SCOPE (non-corpus), and B-02, B-03, B-04, B-09 and B-10 have no corpus source at all — nothing in those five rules was verified by this pass. No cited loc in the range is wrong: every `day-NNN:LNNN` lands on the episode described, though several entries cite the turn that *raised* a question rather than the turn that *settled* it (d018-R20, d004-R18, d019-R19), and several rest on chains whose later locs the tooling never renders.

## Notes on non-FAITHFUL entries

**B-05 / day-018:L5021 — UNDERREACH.** The philosophy entry stops at "process artifacts are therefore not cruft — they are the deliverable", which is exactly the reading the author demolishes 27 lines later: "the documentation in @docs is all AI generated and of varyign quality", CLAUDE.md's techniques are "untested" and possibly "bloated to the point off being counterproductive", and "much of the rest, especially the meta-processual lessons in the docs/analysis is basically just AI slop!" (L5048-L5057). The vindication and the demolition are one exchange. B-31 carries the demolition (via d018-R21) but B-05 gives no sign that its own claim is bounded.

**B-07 / d005-R8 — DISTORTED (embellished-because).** The author's words are "In BookMinder I explicitly stopped you from running ahead with implementing the project with your own creative ideas as happened in BookMind" (day-005:L2228-L2229). The mined `because` renders this as "The predecessor project BookMind **failed**", carrying `because_source: user-verbatim`. Thirteen days later the same author says BookMind "did a ton of things. It actually implemented most of the features skeetched in the inital vision" (day-006:L1793) and B-06 is compiled specifically to forbid the failure framing. The compiled B-07 is clean; the corpus entry is not, and it is the kind of `because` that seeds the next paraphrase.

**B-20 / d008-R19 — DISTORTED (embellished-because).** The author's complaint is "w8, why do you need to run all the bash find commands when `/hi` slash-command should have already executed `tree`?" — a complaint about re-deriving context the session-start command had already produced. The entry converts it into a claim about `.venv` dominating listings, and manufactures the detail "seventeen site-packages paths" out of a collapsed `… +17 lines (ctrl+r to expand)`. The venv-noise lesson is real but its evidence is d006-R4 (day-006:L1109-L1116), a different session filed under a different rule.

**B-21 / d020s5-R7 — OVERREACH.** The author killed a *background `Task`* that was grepping `day-019.md` ("kill that background Task where I see you using grep on claude-dev-log-diary/day-019.md !!!", day-020:L56097). In the four tool calls immediately following, the agent ran `rg -g "day-018.md" -B20 -A20 … | head -200` and three further diary greps in the foreground and drew no objection at all (L56101-L56116). The attested boundary is the background subagent, not sweep breadth; "never a broad `grep … | head -200` sweep" is the entry's own addition and is contradicted by the next twenty lines.

**B-23 / d011-R16 — DISTORTED (misattribution).** The author wrote "I like the status, not the other optional fields (YAGNI for now)." The six-value vocabulary the rule presents as fixed (`backlog, in_progress, in_review, completed, on_hold, research`) is Gemini's proposal list at day-011:L11257-L11262. The compile already caught this and deferred to CLAUDE.md `<backlog_management>`; the corpus entry still carries `because_source: user-verbatim` over an agent-authored enumeration.

**B-24 / d017-R8 — UNDERREACH.** Two things the approved plan (day-017:L1294-L1308) settles are missing from the compiled sentence. First, the plan folds reading status into the same option — `--filter finished`, `--filter unread`, `--filter in-progress` — so the four-value enumeration in B-24 reads as a closed vocabulary when it is a partial one. Second, the mined rule's negative ("there is no `--flag`, `--status`, or `--type`") is dropped entirely, and that negative is the operative half for whoever implements `filter-by-reading-status.yaml` next: without it, adding `--status finished` looks like a reasonable design choice rather than a reversal of a settled decision.

**B-25 / d020s2-R15 — DISTORTED (misattribution).** Both cited locs are the agent reading its own code (`path=""` comment at day-020:L10258) and then editing the TypedDict on its own initiative ("Now let's make the fields optional as the tests suggested we should", L10404-L10418). No author turn appears in either window. The claim is correct and tree-verified; the provenance label is not, and the rule index promotes it further to "user-verbatim".

**B-26 / d020s1-R19 — DISTORTED (embellished-because).** The rule's case list is right and the author's enumeration at day-020:L3317 is quoted exactly. The `because`, though, asserts "Python has no accidental falsy strings to guard against", which the author did not claim — he asked, "(is there a string value that could accidentally be evaluated to false? Or is this a Javascript trauma a Python does't suffer from such automatic conversions)" — and which is false as stated, since `""` is falsy. The rule survives on the author's actual ground: the falsy case is undesirable because there is no product behavior behind it. His refinement one turn later ("3 and 4 is the same and motivation for keeping it is 4, so dump 3", L3325) is the turn that produced the final three-case spec and is not cited.

**B-26 / d020s1-R20, d020s3-R11, d020s4-R10 — one document, three entries, two overreaches.** All three cite a Gemini-written summary of day-010 that the agent pasted into day-020, and which day-020.md re-logs at L5445, L18962 and L29479. Two of them assert more than that document contains: d020s3-R11 adds `corrupted_db_user` (absent from the summary; its real evidence is day-011:L1862), and d020s4-R10 adds the empty-`BKLibrary`-directory detail and the "BKLibrary directory not found" vs "No BKLibrary database found in:" error pair (absent from the summary, and the most useful thing either entry claims). d020s1-R20 is labeled `user-paraphrase` over AI prose. The persona lore itself is sound — the primary evidence is day-010:L1261-L1290, where the author pastes `sudo ls -la` output from a second real Mac account — but B-26's apparent three-source backing is one second-hand document counted three times.

**B-27 / d020s1-R21 — DISTORTED (embellished-because).** The exchange at day-020:L6656 is entirely about redundant `.gitkeep` files ("shouldn't one at the leaf node of our empty directory tree suffice?"). The `because`'s claim that a missing `.gitkeep` let a directory vanish and "kept a test green for the wrong reason for weeks" is not in the window and was not verified anywhere in this pass. The compiled B-27 states only the leaf-`.gitkeep` rule and is unaffected.

## Roll-up — B-05

B-05 currently reads: *The real object of study is whether an AI coding agent can hold a strict ATDD process as a pair programmer; process artifacts are therefore deliverables, not cruft.* Verified as far as it goes — but every one of its sources is a turn where the author qualifies the claim in the same breath. At day-009:L8038-L8041 the benchmark idea and "It might be just turn out ot be just a big  baggage of AI slop in the end" are one sentence apart; at day-018:L5021 he affirms the vehicle framing and at L5056 calls most of `docs/` slop. Read without the qualifier, B-05 is a licence for exactly the artifact inflation the author spent day-018 deleting (12 files, 2,699 lines, commit eea59e3).

Proposed: keep the first clause, and bound the second to the artifacts the transcripts actually protect — *the deliverable process artifacts are the durable chain (story cards → acceptance tests → working code), the session diary, and the agent-instruction files; AI-written retrospective meta-analysis is not one of them (day-018:L5056-L5057, and see B-31)*. Cross-referencing B-31 costs one clause and closes the loop.

## Roll-up — B-20

Two changes, both grounded in windows this pass read.

First, B-20's opening prohibition is attributed to an episode that taught something else. day-008:L2702 is the author asking why the agent ran bash `find` calls when `/hi` had already run `tree` — the lesson is *use the census the session-start command already produced; don't re-derive project structure by hand*. That lesson currently appears nowhere in the skill, and it is a live one for a skill whose whole job is to be loaded at session start. Proposed addition to B-20: *`/hi` (and the skill itself) already supply the tree and the project state — read what you were given before running discovery commands of your own (day-008:L2702).*

Second, the line-count half of B-20 ("never … repo-wide line counts") is well evidenced, but by d006-R4 — `find . -name "*.py" -exec wc -l` returning 531,358 and 294,940 at day-006:L1109-L1116, numbers that became the "44% code reduction" headline the author rejected at day-006:L1793. The rule index files d006-R4 under B-31, whose compiled text has no line-count content. Proposed index correction: move d006-R4's row from B-31 to B-20, and add the concrete failure to the rule's wording (*both repos vendor their venv in-tree; the two numbers that produced the '44% code reduction' claim were almost entirely site-packages*), so the prohibition carries its own falsification.

## Roll-up — B-21

The diary-access clause is the best-evidenced rule in the range — four independent sessions, four different failure shapes — and only one phrase in it is unsupported. B-21 says a grant is scoped to "targeted range reads in the foreground; never whole-file reads, never background-subagent greps". The first and second are attested (day-014:L340-L341 grants one line range; day-014:L417-L419 punishes the whole-file read with "you just wasted 10% of your context on ingesting complete logs"). The third is attested for the *background subagent* only: at day-020:L56097 the author killed a `Task` doing a diary grep, and then watched four foreground `rg` sweeps over diary files without comment (L56101-L56116).

Proposed: keep the sentence, but make the tested boundary explicit rather than implying that any broad pattern search is forbidden — *…the grant is scoped to the excerpt and manner named: range reads over whole-file reads, and never from a background subagent, where the author cannot see or stop what is being read (day-020:L56097)*. The reason is the observable one: he objected to what he could not intervene in.

## Roll-up — B-24

Two changes.

First, restore the negative that the merge dropped. d017-R8's rule ends "there is no `--flag`, `--status`, or `--type`", and the plan the author approved at day-017:L1294 lists `--status finished` → `--filter finished`, `--status unread` → `--filter unread`, `--status in-progress` → `--filter in-progress` (L1305-L1308). B-24's parenthetical `(cloud, !cloud, sample, !sample)` reads as the whole vocabulary; it is the implemented subset. Proposed wording: *one repeatable `--filter` option whose values are attribute names with `!` negation — `cloud`/`!cloud` and `sample`/`!sample` today, with reading status (`finished`, `unread`, `in-progress`) already assigned to the same option by the approved plan; there is no `--flag`, no `--status`, and no `--type`.*

Second, a provenance note the coordinator should carry rather than a wording change: B-24's `list_cmd` clause and its "CLI owns no domain knowledge" clause each cite two corpus entries that are the same episode. day-020.md re-logs whole regions at differing offsets — d020s1-R18 (L2933) and d020s4-R12 (L26198) are the same author turn verbatim, and d020s2-R14 (L9459-L9519) and d020s4-R17 (L23167-L23227) are the same edit sequence verbatim, differing only in whether paths render absolute. Nothing in the rule is wrong; the support is half as broad as the index suggests, and any future mining of day-020 should de-duplicate before counting.

## Roll-up — B-26

B-26's persona list is accurate but drops the one detail that makes the personas distinguishable in behavior. d020s4-R10 records that `never_opened_user` yields "No BKLibrary database found in:" while `legacy_books_user` yields "BKLibrary directory not found" — two different error paths, which is the entire point of having both fixtures, and which requires `never_opened_user` to carry an empty `BKLibrary` directory. B-26 currently describes `never_opened_user` as "only the OS-created `com.apple.iBooksX` container — no BKAgentService, no plist, no sqlite", which reads as *no BKLibrary directory either* and collapses the distinction. Proposed: *`never_opened_user` (the OS-created `com.apple.iBooksX` container with an empty `BKLibrary` directory and nothing else — this is what makes its error "No BKLibrary database found in:" rather than `legacy_books_user`'s "BKLibrary directory not found")*. Flag it for tree confirmation: the claim was not evidenced at its cited loc in this pass, only in the entry itself.

Two supporting corrections. The rule index credits B-26's persona claims to three corpus entries (d020s1-R20, d020s3-R11, d020s4-R10) that all cite one AI-written summary re-logged three times inside day-020.md; the primary source is day-010:L1261-L1290, where the author pastes real `sudo ls -la` output from a second machine, and citing that directly would raise the provenance of the whole page. And d011-R20 — the corrupted-fixture rule whose text B-26 actually carries in its `corrupted_db_user` parenthesis — is indexed under B-27, where nothing uses it; its row belongs in B-26.
