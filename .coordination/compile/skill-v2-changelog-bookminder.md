# Skill v2 changelog — bookminder (judge pass over transcript-level provenance verification)

Judge pass 2026-08-14 over `provenance-verification-bookminder-1.md` (B-01…B-31, 88 corpus rows) and `-2.md` (B-40…B-66, 59 corpus rows). Every verifier proposal was adjudicated; every window behind a rewrite was re-read via `rule.py` before the change, and tree claims were re-checked against HEAD directly. No verifier RETRACTED any source, so **no rule lost its last faithful source and no rule was removed**. Rules changed: 13 (B-05, B-20, B-21, B-24, B-25, B-26, B-41, B-43, B-44, B-47, B-51, B-53, plus a one-clause consistency fix rippling B-25↔B-51). Proposals: 7 accepted, 8 accepted-modified, 0 rejected outright (two sub-options declined inside accept-modified entries, noted below). Index relabels: 8 source clusters. Both sidecars updated; `quick_validate.py` passes.

A form note that applies to several entries: verifier-proposed wordings embedded `day-NNN:LNNN` citations, which the skill's form rules forbid in skill text (provenance lives in the rule index). Every such proposal was accepted **modified** to carry the fact without the citation; the day:loc evidence is recorded here and in the index instead.

## Rule changes

### B-05 — vehicle-and-benchmark claim bounded (accept, report 1 roll-up)
**Old gist**: "process artifacts are therefore deliverables, not cruft" — unbounded. **New gist**: the deliverable artifacts are enumerated (story-cards→acceptance-tests→code chain, session diary, agent-instruction files marked WIP/untested per the author) and AI-written retrospective meta-analysis is explicitly excluded, with the author's own standing caveat ("big baggage of AI slop") attached and a cross-reference to B-31. **Why**: every source of B-05 is a turn where the author qualifies the claim in the same breath; unbounded, the rule licenses exactly the artifact inflation he spent day-018 deleting (12 files, commit eea59e3). **Verification**: report-1 B-05 row (UNDERREACH) + my re-read of day-018:L5048-L5060 ("only docs/apple_books.md is really solid… basically just AI slop", CLAUDE.md "definitely in WIP… techniques… untested") and day-009:L8038-L8041.

### B-20 — the /hi-census lesson restored; line-count prohibition carries its falsification (accept both, report 1 roll-up)
**Old gist**: opened with "read the ~290 lines"; the venv/line-count prohibition was bare. **New gist**: opens with "`/hi` (and this skill) already supply the session-start census — read what you were given before running discovery commands"; the line-count prohibition now names its falsification (both repos vendor a venv in-tree; the 531,358/294,940 counts behind the rejected "44% code reduction" headline were almost entirely site-packages). **Why**: d008-R19's actual episode (day-008:L2702, "why do you need to run all the bash find commands when `/hi` … should have already executed `tree`?") taught census-reuse, not venv noise — that lesson previously appeared nowhere in the skill; the venv-noise evidence is d006-R4 (day-006:L1109-L1116), which was mis-filed under B-31. **Verification**: report-1 B-20 row (DISTORTED, embellished-because) + my re-read of both entries. Index: d006-R4 re-filed B-31→B-20 (disposition updated to match).

### B-21 — diary-grant scope corrected to the tested boundary (accept, report 1 roll-up)
**Old gist**: grant scoped to "targeted range reads in the foreground; never whole-file reads, never background-subagent greps" — implying any broad grep is forbidden. **New gist**: range reads of the named lines, not whole-file ingests (the 10%-of-context whole-file read named as the enforcement); never from a background subagent, where the author cannot see or stop what is being read. **Why**: at day-020:L56097 the author killed a background `Task` grepping day-019.md, then watched four foreground `rg … | head -200` diary sweeps (L56101-L56116) without objection — the attested boundary is observability/stoppability, not sweep breadth. **Verification**: report-1 B-21/d020s5-R7 row (OVERREACH) + my re-read of the full window.

### B-24 — dropped negative restored; "repeatable" corrected (accept-modified, report 1 roll-up)
**Old gist**: "one repeatable `--filter` option … (`cloud`, `!cloud`, `sample`, `!sample`)" — reads as the whole vocabulary, and "repeatable" is untrue at HEAD. **New gist**: one `--filter` option, **single-value at HEAD** (multiple filters are deferred future work with no story yet); `cloud`/`!cloud`, `sample`/`!sample` implemented today with reading status (`finished`, `unread`, `in-progress`) already assigned to the same option by the approved plan; **no `--flag`, no `--status`, no `--type`** — reintroducing one is reopening a settled decision. **Why**: the approved plan (day-017:L1294-L1308) folds reading status into `--filter` and the mined rule's operative negative had been dropped in compile; without it, `--status finished` looks like a design choice to the next implementer. Modification beyond the proposal: I removed "repeatable" — cli.py:40-44 at HEAD has no `multiple=True`, and the author's own steer (day-017:L1288-L1290) explicitly deferred multiple filters. **Verification**: report-1 B-24 row (UNDERREACH) + my re-read of day-017:L1274-L1314 + cli.py at HEAD.

### B-25 — TypedDict WHY made accurate (consequential fix, no proposal)
**Old gist**: "`path` and `updated` are `NotRequired` because the SQLite path cannot supply them". **New gist**: the type guarantees only what every code path can honestly supply; the DB-backed path fabricates `path` as `""`. **Why**: library.py:74 shows `updated` IS supplied by the DB path (`ZLASTOPENDATE`) — only `path` is fabricated; the old WHY was false for `updated` and inherited B-51's overreach for `path`. **Verification**: library.py:69-78 at HEAD. Index: d020s2-R15 relabeled agent-synthesis (report-1 DISTORTED/misattribution row: both cited locs are the agent reading/editing its own code, no author turn).

### B-26 — never_opened_user's empty BKLibrary directory + error-path pair, upgraded to verified@tree (accept-modified, report 1 roll-up)
**Old gist**: "`never_opened_user` (only the OS-created `com.apple.iBooksX` container — no BKAgentService, no plist, no sqlite)" — reads as "no BKLibrary directory either", collapsing the distinction from `legacy_books_user`. **New gist**: the container holds an **empty `BKLibrary` directory** and nothing else; a new sentence states the two error paths the legacy/never-opened split exercises (missing dir → "BKLibrary directory not found", empty dir → "No BKLibrary database found in:") and why the leaf `.gitkeep` is load-bearing. **Modification**: the verifier flagged this detail as unverified-at-cited-loc (it came from the third re-log of a Gemini summary) and asked for tree confirmation — I confirmed it directly instead of hedging: at HEAD the fixture tree has `never_opened_user/…/BKLibrary/.gitkeep` and `legacy_books_user` has no BKLibrary directory, and library.py:26-37 raises exactly those two messages. Stated as verified@tree. **Verification**: report-1 B-26 roll-up + `git ls-files` of both persona trees + library.py:24-39 at HEAD.

### B-41 — two over-strong sentences hedged to what the transcripts hold (accept-modified, report 2 roll-up)
**Old gist**: (a) "the plist omits cloud-only titles, so absence proves nothing about library membership"; (b) "`updateDate` in the plist is publisher-revision metadata, not reading activity". **New gist**: (a) titles the database knew were demonstrably missing from a **fresh** `plutil -convert swift` snapshot and the cause was never established (author's verdict: "still a mystery") — so plist absence proves nothing in either direction; (b) `updateDate` is **probably** publisher-revision metadata — the author's never-verified hypothesis; what the episode settled is that the `sort_by='updated'` feature was deleted as YAGNI because the field's meaning was unknown. **Why**: (a) welded an unexplained observation to a mechanism (day-013:L871-L872); (b) flattened a triple-hedged author guess (day-007:L2708-L2710, "I'm uncertain… probably… I guess…?") into schema lore and dropped the only durable decision (L2739/L2749). Modified only to strip day-refs from skill text. **Verification**: report-2 d013-R18 (OVERREACH) and d007-R17 (OVERREACH) rows + my re-read of both windows. Index: d008-R9 relabeled agent-synthesis (day-008's conversation destroyed by Cursor scrollback, day-008:L1521 — only the agent's doc diff survives).

### B-43 — the ZSTATE-5 chronology was inverted; span rewritten (accept-modified, report 2 roll-up — the headline fix)
**Old gist**: "earlier research recorded it as 'Series entity / unowned series book' … but a **later systematic re-probe** observed 5 only as a second row for a title that already had a 3, **which the series story does not predict**". Both halves wrong. **New gist**: two observations **in this order, consistent with each other without settling it** — the batch census (Attack Surface at 3 and 5) came FIRST (day-014:L742/L747); the `ZSERIESID` join came LATER and was the more systematic probe (day-015:L1708-L1715: owned book at 1, `Hainish|MultipleAuthors|5|0`, two unowned Le Guin member titles at 5, all sharing one series id) — a structure that would also produce exactly that duplicate row. Added: the author's framing left the entity/member relation unknown (day-015:L1643-L1645); ~206-row population (day-015:L1539); the docs' ZTITLE discriminator is the agent's own substitute, written after the author rejected a ZAUTHOR heuristic and pointed at "entries and seriesid" (day-015:L2084-L2108). Also: opening sentence no longer claims "5 means series" was "contradicted" (the join supported it), and "the method that produced **every** real breakthrough" is now "the method that produced **the ZSERIESID** breakthrough" — the corpus attests one. **Verification**: both windows re-read in full this pass; the inversion is unambiguous (day-014 precedes day-015; L1721 records the join as confirming the author's series hypothesis, not contradicting it). Index: d014-R3 and d015-R7 relabeled agent-synthesis; d012-R11/d013-R15 "two ZSTATE=5 rows" because-error noted.

### B-44 — one-episode evidence named; lifecycle relay-hedged; revert rationale corrected (accept-modified, report 2 roll-up)
**Old gist**: (a) "Per-title sample observations flipped between probes … (the same title read `ZISSAMPLE 0` one session and `1` another)" — describing as instability what the previous sentence explains as lifecycle; (b) divergence clause ended "know this before 'fixing' either side without a spec", inheriting d016-R17's invented "reverted for lacking a failing spec" rationale. **New gist**: (a) per-title values are time-varying, not noise — `(6,0)` → `(1,1)` after opening IS the proposed mechanism; freshness stamp + re-run query still mandatory; (b) the 3-only filter is where the author's revert of an **unauthorized** widening left it ("I didn't stop you in time", day-016:L7169-L7170) — he did not rule the wider predicate wrong; change neither side without his direction and a spec; (c) lifecycle hypothesis now says it is reconstructed second-hand in a relayed log analysis, never probed live. **Verification**: report-2 d016-R17 (DISTORTED) and d019-R14/R16 (DISTORTED, misattribution) rows + my re-read of day-016:L7166-L7190; the day-012 probes (d012-R13) were verified FAITHFUL. Index: d012-R13 marked primary with d013-R17 (replay) and d019-R14 (Gemini summary) folded into it — the predicate's independent evidence is d012-R13 + d018-R16, not four episodes.

### B-47 — counter-observation carried; provenance honestly second-hand (accept, report 2 roll-up)
**Old gist**: hedged "appears never to populate" but silent on source quality and on the same-session counter-observation. **New gist**: both claims flagged second-hand (relayed log summary + agent's own requirements reasoning, never a live probe); counter-observation added — the UI renders some cloud samples at 1% (B-53's UI-quirk clause), so the untested part is precisely the database-side `ZREADINGPROGRESS = 0.0` claim. **Why**: day-019:L1616-L1617, 700 lines into the same session B-47 is mined from: "Samples can occasionally have progress, contrary to my initial assumption." **Verification**: report-2 d019-R17 row (DISTORTED, misattribution) + my re-read of both windows. Index: relabeled agent-synthesis (Gemini relay).

### B-51 — stamp split: code fact vs never-checked schema claim (accept, report 2 roll-up)
**Old gist**: headline "(verified@tree, library.py:73)" over "the database has no usable path". **New gist**: what is verified@tree is `path=""` pending ZASSETID correlation; whether the database has any usable path column **was never checked** — left as an acknowledged TODO at the author's direction — so "path exists only in Books.plist" is a needs-live-DB guess, not established schema. **Why**: the only transcript basis is the agent's "might not have path info" (day-009:L4459) and the author's instruction was precisely to record the open question (day-009:L4436-L4438). **Verification**: report-2 d009-R23 row (OVERREACH) + my re-read of the window.

### B-52 — no-print-debugging clause kept, its real sources cited (accept-modified, report 2 roll-up)
The verifier offered: cite the clause's actual sources or drop it. **Decision**: keep — it is corpus-true, author-enforced ("DO NOT FUCKING TOUCH OUR IMPLEMENTATION for exploratory probing"), and belongs in the query-discipline rule where a reader needs it. The index row now cites d015-R1 + d014-R6 as a deliberate cross-skill echo of tdd-bdd T-96 (both verified present in corpus and in the tdd-bdd index). Skill text unchanged.

### B-53 — named speculative sections restored (accept, report 2 roll-up)
**Old gist**: "a running record of guesses" — true but unactionable. **New gist**: adds that the doc's "Edge Cases" and NULL-handling sections, like its `ZCONTENTTYPE` mapping (B-45), are speculation written in the same voice as its observations. **Why**: d008-R11 named them and recorded that the speculative half later seeded a real defect; the compile had generalized the names away. **Verification**: report-2 d008-R11 row (FAITHFUL, more specific than compiled) + my re-read of the entry.

## Rejected / declined

No verifier proposal was rejected outright. Two sub-options were declined inside accept-modified adjudications:

- **B-52 drop-the-clause option** — declined in favor of citing the clause's real sources (above); dropping a true, author-enforced prohibition to fix a bookkeeping gap would optimize the index at the reader's expense.
- **B-26 "flag for tree confirmation" hedge** — declined as unnecessary; I performed the tree confirmation instead and stated the error-path pair as verified@tree (fixture trees + library.py:24-39 at HEAD).

Also considered and NOT changed (corpus distortion did not reach the compiled text; recorded as index annotations only): B-07 (corpus `because` says "BookMind failed"; compiled rule and B-06 already forbid that framing), B-23/d011-R16 (Gemini's six-value vocabulary mislabeled user-verbatim; compile already defers to CLAUDE.md `<backlog_management>`), B-26/d020s1-R19 ("Python has no falsy strings" invented in the corpus `because`; the compiled "exactly three cases, no falsy-string case" is a statement about the spec and is tree-accurate — re-checked against library_spec.py:37-49), B-27/d020s1-R21 ("kept a test green for weeks" unevidenced; compiled rule carries only the leaf-`.gitkeep` form).

## Systemic corrections

1. **day-020 double-counting deduplicated.** Verified identities recorded in the index preamble and rows: d020s1-R18 = d020s4-R12 (one author turn), d020s2-R14 = d020s4-R17 (one edit sequence; disposition row reclassified merged-duplicate, tallies 119 compiled + 10 merged), d020s1-R20 = d020s3-R11 = d020s4-R10 (one Gemini-written day-010 summary re-logged three times). B-24's and B-26's apparent source breadth was overstated by exactly these copies.
2. **Gemini-relay text relabeled.** d019-R14, d019-R16, d019-R17, and the d020s1-R20 cluster carried `user-paraphrase`/`user-verbatim` labels over text a third-party agent wrote and the author pasted with "I have asked Gemini … Following is it's reponse". All relabeled **agent-synthesis (Gemini relay)** in the index; where relay material is a fact's only support, the skill text now hedges (B-44 lifecycle, B-47). The persona lore itself keeps its primary source: day-010:L1261-L1290, the author's own `sudo ls -la` paste. Additional misattribution relabels: d008-R9 (day-008 conversation destroyed; doc-diff residue), d020s2-R15 (agent's own code archaeology), d014-R3 and d015-R7 (miner synthesis over an author turn that said something narrower). Total relabels: 8 (d008-R9, d014-R3, d015-R7, d019-R14, d019-R16, d019-R17, d020s1-R20 cluster, d020s2-R15).
3. **Index mis-filings fixed.** d006-R4 re-filed B-31→B-20 (its line-count content is what B-20's prohibition is made of; B-31 uses none of it) — disposition ledger updated to match. d011-R20 re-filed B-27→B-26 (its content lives in B-26's `corrupted_db_user` parenthesis; disposition already said B-26).
4. **Retractions: none.** No source was RETRACTED by either verifier; every cited day:loc landed on its described episode. Consequently no rule was left without faithful support and **no rule was removed**.
5. **Corpus-unverified rules marked.** B-02, B-03, B-04, B-09, B-10, B-45, B-60, B-61, B-63, B-64, B-65, B-66 rest entirely on threads.md rulings / ratified ledger / tree verification — author-attested, out of the transcript pass's scope. Marked **corpus-unverified (author-attested)** in the index (B-62's one corpus source d021-R14 was verified FAITHFUL; B-01's and B-22's corpus halves likewise). They were not touched: the marking is a status for the next verification era, not a defect.
