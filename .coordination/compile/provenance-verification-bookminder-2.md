> NOTE (2026-08-15): day-007 line refs in this report cite the pre-repair day-007.md (blob 2b1776656183cce5c73dd82a7c89eed6c69eeec0). Locs ≤6213 are unchanged in the repaired file; locs 7660-7661 resolve to 3116-3117; locs ≥7664 correspond to repaired line minus offset — subtract 1450. See .coordination/tools/day-007-remap.md.

# Provenance verification — bookminder skill, B-40 … B-66

Transcript-level verification of every corpus source behind `references/apple-books-domain.md` (B-40 … B-53) and `references/current-state-and-open-decisions.md` (B-60 … B-66). Each entry was re-read against the primary transcript at its evidence loc (~60 lines each side, `rule.py -r <id> -c 60`), on the four axes of the P-12/P-14 pilot: quote exactness, episode fidelity, generalization fit, missed context in the read window. 59 corpus rows.

Out of scope, per brief (non-corpus sources only): **B-45** (ledger L-12 + `docs/apple_books.md` line comparison), **B-60** (verified-residue pointer + threads.md convention + CLAUDE.md), **B-61** (ledger L-11 + tree re-verification), **B-63** (verified-residue R1 + threads.md:107 + tree), **B-64** (ledger L-14..L-19), **B-65** (threads.md + skill-removals fragments), **B-66** (threads.md:96-97). **B-62** carries one corpus source (`d021-R14`), verified below; its ledger-L-13 and tree-verified halves are out of scope.

No id collision exists in this range: every source is `d`-prefixed and `pairing/` contains no `d`-prefixed ids (only `p010/p019/p021` series, all of which also exist in `v2/`). One tool caveat: `.coordination/mining/v2/day-020-s2.yaml` quotes its ids (`- rule_id: "d020s2-R15"`), which `rule.py`'s `ID_LINE` regex cannot match, so `-r B-51` reports `NOT FOUND IN CORPUS: d020s2-R15`; the entry exists at `day-020-s2.yaml:L214` and was read directly. A second tool caveat: `print_transcript_windows` only windows locs written as `day-NNN:LNNN`, so bare follow-on `LNNNN` references in an `evidence` field produce no window — those were read directly from the diary files and are cited as such below.

| rule | source | quote | episode | generalization | missed context | verdict |
|------|--------|-------|---------|----------------|----------------|---------|
| B-40 | d002-R12 | no `quote` field | matches: container census @ day-002:L1914-L1937 shows `Books.plist` beside `<numeric-id>.epub` directories; `file` reports "Apple binary property list" @ L1979; plutil+plistlib bridge @ L2140-L2149 (read directly) | follows; compiled rule keeps only the location half and drops the superseded bridge (correctly — B-50 carries the reversal) | L2173-L2175: the same code skips books whose path does not exist on disk — the defect d005-R7/B-41 later reverses | FAITHFUL |
| B-40 | d008-R8 | exact @ day-008:L1570 | matches: author flags the numeric segments, agent confirms `BKLibrary-*` carries them and `BCRecentlyOpenedBooksDB.sqlite` does not (L1602-L1626) | follows | none | FAITHFUL |
| B-41 | d008-R9 | no `quote` field | cited loc is a line *inside a doc diff the agent wrote* ("BKLibrary database is the authoritative source for reading progress"), not an exchange; no author turn exists in the window | rule follows and is independently corroborated by d009-R20 | L1521: the day-008 conversation was lost to Cursor's scrollback ("The complete history of my conversation with Claude is lost") — the diary here is diff residue, so no `user-paraphrase` can be recovered | DISTORTED(misattribution) |
| B-41 | d009-R20 | no `quote` field | matches exactly: `grep -c BKPercentComplete` → 22 @ L231-L234, `sort -u` → single `1.000000` @ L240-L243, `grep -c '"itemName"'` → 179 @ L249-L252 | follows | none | FAITHFUL |
| B-41 | d012-R14 | no `quote` field | the cited loc is the agent asserting the *opposite* — absence from `All_Books.swift` "further supports the hypothesis that ZSTATE=5 indicates a cloud-only book" (day-012:L2413-L2419). The entry mines the corrected lesson but cites the error as if it were the finding | rule follows (as an anti-pattern) | the correction that makes the rule authoritative is the author's "You're jumping to conclusions" (day-013:L866, quoted by him verbatim from day-012) — not cited by this entry | UNDERREACH |
| B-41 | d013-R18 | no `quote` field | matches: fresh `plutil -convert swift` snapshot still lacked "Lao Tzu: Tao Te Ching" (day-013:L848-L860), whose live row reads `ZSTATE: 3` @ L882 | the `because` ("plist absence proves nothing about library membership") is conservative and follows; the rule sentence's "omits cloud-only titles" asserts a mechanism | L871-L872: the author's own verdict on the omission is "still a mystery to us. Something's fishy there" — the cause was never established | OVERREACH(mechanism asserted where the episode records an unexplained anomaly) |
| B-41 | d007-R17 | exact @ day-007:L2708-L2710 (wrap), inside parentheses in the original | matches: author questions the `updated` sort | the author's turn is explicitly hedged — "I'm uncertain about the semantics of the updated field ... it's probably related to Book's revision ... ? I guess these are store in the SQL db ...?" — the rule and the compiled sentence state it flatly; the `evidence` note's "confirmed by HEAD" confirms only the second clause (user interaction lives in the DB), not the publisher-revision reading | L2739/L2749: the actual outcome was deleting the sort feature as YAGNI *because* the semantics were unclear — the episode's lesson is "don't ship a feature over an unknown field", which neither entry nor compiled rule carries | OVERREACH(dropped hedge) |
| B-41 | d005-R7 | exact @ day-005:L2147-L2148 (wrap) | matches: test fails on the second machine, author diagnoses lazy iCloud download, agent locates the `os.path.exists` guard @ L2179-L2181 | follows | the same author turn establishes the GOOS unit/integration vocabulary (L2148-L2150) — tdd-bdd material, not B-41's | FAITHFUL |
| B-42 | d008-R10 | no `quote` field | matches: `apple_to_unix_timestamp` with the 978307200 difference written into the doc @ day-008:L2099-L2103 | follows | none | FAITHFUL |
| B-42 | d009-R22 | no `quote` field | matches: `_apple_timestamp_to_datetime` extracted @ day-009:L3636-L3645 | follows | L3641-L3642's `datetime.min` for `None` is removed one session later per d009-R21 — not carried, correctly | FAITHFUL |
| B-43 | d014-R3 | no `quote` field | the batch @ day-014:L735-L764 shows values 1/3/5/6 and 5 only as a second row for "Attack Surface" (which already has a 3) — verified; but the batch shows *numbers*, not the semantics ("1 = present locally, 3 = cloud-only, 6 = sample") the rule attributes to it, and the interpretive table was never produced (session killed @ L782) | rule follows from the corpus as a whole | the only author turn in the window is L721-L722 (Postman is cloud-and-not-downloaded; "Can't you do this more efficiently?"); the `because`'s "every earlier single-value story was contradicted" narrative — including the series story, contradicted on day-015 — is the miner's, yet labelled `user-paraphrase` | DISTORTED(misattribution) |
| B-43 | d011-R19 | no `quote` field | matches: the ZSTATE 5 group @ day-011:L7422-L7628 is dominated by Cars/Disney/public-domain classics, "Apple Books Classics" (L7470), "Ramses" (L7429), "Dune" (L7431), "The Harry Potter Series" (L7524, read directly), "The Baroque Cycle"/"Hainish" (L7613-L7616) | follows | the same group contains individual *Harry Potter books* alongside the series row — direct support for the "entity **and** unowned member" reading the compiled rule uses | FAITHFUL |
| B-43 | d012-R11 | exact @ day-012:L2628-L2629 | matches: author's screenshots, agent reverts its fixture INSERT @ L2653-L2657 and hedges the doc @ L2681-L2683 | the author's wording is "ZSTATE=5 **seems like** 'Series'" and the agent's own doc edit says "might indicate"; the rule flattens both to "Treat ZSTATE = 5 as a Series/collection entry" | the `because` says "the two ZSTATE=5 rows in the author's library" — contradicted by day-011:L7422ff (dozens) and day-015:L1542 ("206 books with ZSTATE = 5") | DISTORTED(embellished-because) |
| B-43 | d012-R12 | no `quote` field | matches exactly: the summary @ day-012:L2956-L2962 lists ZSTATE 3 (Lao Tzu, Tao Te Ching, record-layer-paper, Canon of Reason, Quicksilver), 6 (Snow Crash), 1 (What's Our Problem?), all cloud-iconed | follows | none | FAITHFUL |
| B-43 | d013-R15 | exact @ day-013:L827-L828 | the quote is the author's day-012 turn, replayed by him | the quote says ZSTATE-5 rows "look like **suggested books in a series** by the same author" — i.e. individual unowned titles. The rule asserts the opposite: "Series containers ... **not** as individual books". The container reading comes from a *later* author turn (day-015:L1643, "State 5 indicates the Series itself"), which this entry does not cite | `because` repeats the "two ZSTATE 5 rows" error (see d012-R11); the author's own day-015 framing leaves the entity/member relation explicitly unknown | DISTORTED(misattribution) |
| B-43 | d013-R16 | no `quote` field | matches: replayed day-012 summary @ L1284-L1300, agent's revised reading @ L1400-L1408, code mismatch (5 in the query, 3 in `_row_to_book`) @ L682-L683 | follows | none | FAITHFUL |
| B-43 | d015-R6 | no `quote` field | matches exactly: `SELECT DISTINCT ZSTATE` → 3,1,5,6 @ day-015:L1371-L1378; 206 rows at 5 @ L1539-L1542; the ZSERIESID join returning `Hainish|MultipleAuthors|5|0` plus two unowned Le Guin titles beside the owned `ZSTATE=1` book @ L1708-L1715 (read directly) | follows; "NULL is not a legal value" softens correctly to nothing in the compiled rule | the author's DB-first directive that produced this is at L1314-L1318, credited to d015-R12 not here | FAITHFUL |
| B-43 | d015-R7 | exact @ day-015:L2084-L2085 | matches: agent proposes the `ZAUTHOR`/"MultipleAuthors" heuristic @ L2074-L2075, author rejects it | the author named a *different* discriminator — "it must be something with the **entries and seriesid**". The rule's positive half (use `ZTITLE`) is the agent's own substitute @ L2092-L2094, never endorsed in the window, yet the entry is `because_source: user-verbatim` and the `because` says the author "pushed the discriminator onto the entry/seriesid structure" — which the rule then does not do | the next author turn (L2132) is about fixture data, not this; the ZTITLE discriminator was written into `docs/apple_books.md` @ L2099-L2108 unchallenged | DISTORTED(misattribution) |
| B-43, B-44 | d016-R17 | no `quote` field | matches: the "Verified ZSTATE Mappings" doc block written @ day-016:L5236-L5242; the 3-or-6 widening reverted to 3 @ L7178-L7186 | mapping half follows | the author's stated reason for the revert is "I see that I didn't stop you in time and you've already added 6 to the implementation put it into previous state" (L7169-L7170) — a premature-change objection. "explicitly reverted **for lacking a failing spec**" appears nowhere at either cited loc | DISTORTED(embellished-because) |
| B-43 | d015-R12 | exact @ day-015:L1317-L1318, typo "shoud" preserved | matches: agent abandons the fixture as a research substrate @ L1342-L1351, enumerates distinct values @ L1371, and the ZSERIESID breakthrough lands @ L1708-L1715 | follows | the same author turn also asks for a fixture built by reduction from his real database — fixture-architecture material (B-27), not B-43's | FAITHFUL |
| B-44 | d019-R14 | exact @ day-019:L850 | the cited text is **Gemini's log-analysis summary**, pasted by the author as "I have asked Gemini ... Following is it's reponse" (L794-L796); corroborating locs L936, L949-L951, L959-L960 are the same document | rule follows; the underlying observation is day-012's | `because_source: user-paraphrase` labels a third-party agent's summary relayed by the author. The primary evidence is d012-R13 (author verbatim), which this entry does not reach through | DISTORTED(misattribution) |
| B-44 | d019-R15 | no `quote` field | matches: agent reasons `ZSTATE=6` is cloud @ day-019:L4407, implements `is_cloud=row["ZSTATE"] in (3, 6)` @ L4960 | follows | the author's turn @ L4413 is "That's a good fix, **but let's listen to our tests** ... there should be a unit test to define this" — the endorsement is real but the substantive instruction (drive it from a unit spec, which then happened @ L4952-L4967) is dropped | FAITHFUL |
| B-44 | d019-R16 | no `quote` field | matches the text at the locs: proposed lifecycle @ L963-L978, written into the doc @ L1100-L1104 | follows; compiled rule correctly stamps it hypothesis | same as d019-R14 — the lifecycle is Gemini's reconstruction of a day-017 observation, relayed in a day-019 paste, labelled `user-paraphrase`; no live before/after probe appears in this window | DISTORTED(misattribution) |
| B-44 | d012-R13 | exact @ day-012:L2975 | matches: author's correction, then live probes returning `ZISSAMPLE: 0` for Tiny Experiments @ L2986 and Snow Crash @ L2997 | follows | none | FAITHFUL |
| B-44 | d013-R17 | no `quote` field | matches: the *same* day-012 exchange replayed inside day-013's recap @ L1313-L1315, L1350, L1357-L1361, plus the agent's summary @ L1407-L1408 | follows | this is not an independent episode — it is d012-R13's turn quoted back; the two entries are one observation counted twice | FAITHFUL |
| B-44 | d018-R16 | exact @ day-018:L2198-L2199 (wrap), parenthetical preserved | matches: agent's spec asserted `ZISSAMPLE=1` only @ L2190-L2195, author objects, agent queries and gets 15 `ZSTATE=6` rows @ L2209-L2212 | follows | none | FAITHFUL |
| B-46 | d014-R4 | no `quote` field | matches exactly: `Attack Surface|Cory Doctorow|3|0` and `|5|0` @ day-014:L659-L660 and again @ L742/L747; `A Clockwork Orange` under two author strings @ L759-L760 | follows | none | FAITHFUL |
| B-46 | d011-R18 | no `quote` field | matches: agent's finding @ day-011:L7653-L7654, incl. "The Making of the Atomic Bomb", Progress: 0.79, IsFinished: 1 | follows | the entry's own `feasibility` note already records the elision caveat; the window confirms it — the raw rows are the author's paste, not visible output | FAITHFUL |
| B-46 | d009-R21 | evidence string is character-exact against L5574-L5575 (two lines joined) | matches: author asks "can ZLASTOPENDATE be null?" @ L5531, then "it looks impossible, but check in the real db directly" @ L5564; probe returns 221/0; guard and `float \| None` removed @ L5599-L5607 | follows | L5564 is an unattributed author-verbatim for the DB-first method (B-43's last sentence, B-52) — evidence available and uncredited | FAITHFUL |
| B-47 | d019-R17 | no `quote` field | the cited L840-L844 is Gemini's day-017 log summary pasted by the author; L1546 and L1602 (read directly) are the agent's own requirements dialogue and thinking — no author turn stands behind any of the three | rule follows; compiled B-47 correctly re-hedges to "appears never to populate" | day-019:L1616-L1617, in the same session: "Samples can occasionally have progress ... The UI shows some samples with minimal progress, like 'Tiny Experiments' at 1%." The qualifier is dropped here and survives only obliquely in B-53's UI-quirk clause | DISTORTED(misattribution) |
| B-48 | d015-R8 | exact @ day-015:L1642-L1643 (box-drawn wrap) | matches: agent's unread-only hypothesis @ L1632-L1633, author adds samples and asks for the ordering. The seven-title composition table is real @ L1888-L1898 (read directly), so "all seven observed titles" holds even though the first probe returned only four (L1607) | follows | the "arithmetic error" is confirmed: the human-readable conversions @ L1913-L1919 are wrong (raw `ZCREATIONDATE` 767720827 vs 768637810 differ by ~10.6 days, rendered as the same second), which is what produced the "identical timestamp / unknown tie-break" conclusion @ L2040-L2043, accepted by the author with "OK" @ L2059 | FAITHFUL |
| B-48 | d018-R22 | exact @ day-018:L6120-L6121 (wrap), typo "aren" preserved | matches: doc said "Books marked for future reading" @ L6112, corrected to "Books that Apple Books algorithmically suggests (exact criteria unknown)" @ L6137 | follows | none | FAITHFUL |
| B-49 | d010-R18 | no `quote` field | matches: fresh account after first launch shows `Books.plist` and `BKLibrary-*.sqlite` both stamped 00:12 @ day-010:L1152-L1165, empty `Books` array @ L1168-L1177, `COUNT(*)` → 0 @ L1221 | follows | the "iBooksX created at account setup" clause is *not* attested at the cited L1182 (which is only about first launch); it is attested at L961-L967 (read directly), where the logged-in-but-never-opened account already has `com.apple.iBooksX` and no BKAgentService. Citation gap, not a distortion — the author had challenged the agent's earlier guess at L1083 before it was tested | FAITHFUL |
| B-49 | d010-R19 | exact @ day-010:L1120 | matches: author explicitly tests the no-Apple-ID hypothesis after challenging the agent's assumption @ L1083-L1084 | follows | none | FAITHFUL |
| B-49 | d010-R22 | exact @ day-010:L2781 | matches: author's paste of a real run; `sudo` variant on the next line succeeds into "Apple Books database not found" @ L2782-L2784 | follows | none | FAITHFUL |
| B-49 | d020s1-R20 | no `quote` field | matches @ day-020:L5445, and the facts are independently verified at day-010 (L961-L967, L1152-L1165) | follows | the loc is a Gemini log-research summary the author demanded verbatim ("no, first show me also the verbatim response", L5432); `user-paraphrase` is loose but the substance is primary-verified elsewhere | FAITHFUL |
| B-50 | d010-R20 | no `quote` field | matches exactly: "test_reader has Books as a dict (keyed by book ID) / test_home (the original one) has Books as an array" @ day-010:L6211-L6213, and "Our code expects Books to be a list/array! ... which is why it returns an empty list" @ L6220 | follows | none | FAITHFUL |
| B-50 | d007-R16 | exact @ day-007:L7660-L7661 (wrap), typo "plistutil" preserved, trailing parenthetical elided | matches: research task @ L7665, "The plutil conversion is completely unnecessary" @ L7668, code simplified @ L7671 | follows | none | FAITHFUL |
| B-50 | d009-R24 | exact @ day-009:L174 | matches: author prepared the Swift dumps for token efficiency; used immediately @ L231-L266 | follows | the author's own flag is `plutil -fmt swift`; the working invocation is `-convert swift` (d012-R15). Compiled B-50 uses the correct form | FAITHFUL |
| B-50 | d012-R15 | exact @ day-012:L2485 | matches: the author pastes the `plutil` man-page ARGUMENTS block after two agent denials that plutil can emit Swift (L2453-L2463, L2467-L2472); conversion runs @ L2492 | follows; `needed-enforcement` is accurate — the author had to say "I told you to check man plutil" @ L2465 | none | FAITHFUL |
| B-50 | d002-R12 | (second listing) | see B-40 row | compiled rule correctly marks the plutil-bridge method superseded | none | FAITHFUL |
| B-51 | d009-R23 | exact @ day-009:L4437-L4438 | matches: author's question, no answer available, TODO written into the code @ L4480 | the rule states "The book file path is **not** in the BKLibrary database" as fact; the window contains only the agent's "we query BKLibrary database which **might** not have path info" @ L4459. No schema check occurs. The compiled rule inherits the flat claim under a `verified@tree` headline, with the schema half stamped needs-live-DB only in the index | the author's own instruction was to leave a FIXME/TODO for an unanswered question — the durable lesson (acknowledged debt, not fact) is what the compiled sentence's last clause preserves | OVERREACH(unverified schema claim stated as fact) |
| B-51 | d020s2-R15 | no `quote` field | matches: `path="",  # Path requires Books.plist correlation with ZASSETID` @ day-020:L10258; `path`/`updated` made `NotRequired` @ L10416-L10417 | follows | id is unresolvable by `rule.py` because it is quoted in `day-020-s2.yaml:L214` — tool defect, entry present | FAITHFUL |
| B-52 | d016-R14 | exact @ day-016:L3737 | matches both halves: "can't you do this with runnig sqlite3 from command line, without writing temporary files?" @ L3226 (rejecting a `WriteFile examine_duplicates.py` @ L3222), and the `SELECT *` objection @ L3737 | follows; `repeated` is accurate | none | FAITHFUL |
| B-52 | d012-R16 | no `quote` field | matches: three consecutive escape failures @ day-012:L2885, L2905, L2926, then a parameterized query succeeds @ L2938 | follows | none | FAITHFUL |
| B-52 | d014-R5 | exact @ day-014:L572 | matches: agent's query on a diacritics-bearing title returns nothing, author supplies the ASCII prefix, row found @ L577, agent concludes "The issue was indeed with special characters in the title" @ L580 | follows; "the shell round-trip, not the database" is the miner's attribution, hedged in the entry as "shell/sqlite round-trip" | none | FAITHFUL |
| B-52 | d014-R8 | exact fragment @ day-014:L721-L722 (wrap) | matches: 20+ single-title probes, author objects, agent switches to `WHERE ZTITLE IN (...)` @ L728-L735 | follows; only the batching kernel is used here, correctly (entry's own `skill_target` is pair-programming) | the same turn carries a ground-truth correction about Postman being cloud-and-not-downloaded, used by B-43 | FAITHFUL |
| B-52 | d015-R12 | (second listing) | see B-43 row | follows | none | FAITHFUL |
| B-53 | d008-R11 | exact @ day-008:L4234, unclosed parenthesis preserved | matches: the doc is committed as the record of the exploratory phase | follows | the entry additionally names "Edge Cases", NULL-handling and `ZCONTENTTYPE` as the speculative sections; the compiled rule generalizes to "a running record of guesses" and keeps only `ZCONTENTTYPE` (via B-45) | FAITHFUL |
| B-53 | d014-R2 | exact @ day-014:L214-L215 (wrap) | matches: author orders a row-by-row verification of the documented Previous Section against the screenshots | follows | none | FAITHFUL |
| B-53 | d019-R20 | exact @ day-019:L1176, typos preserved | matches: agent was adding "-- Include samples in recent reading" SQL to the doc @ L1174 when the author objected | follows | none | FAITHFUL |
| B-53 | d021-R17 | exact @ day-021:L6947, truncated mid-sentence without an ellipsis (the original continues "that replicated the list in our testing") | matches: agent had been stripping column names out of the UI sections @ L6864-L6872 and centralizing the query @ L6900-L6902 | follows | the `because` ("the same query in two sections is what let the two copies contradict each other") is the miner's compression of the drift sweep @ L6919-L6942, where the agent finds "UI Behavior Insights" outdated — plausible but not an author statement at the cited loc | FAITHFUL |
| B-53 | d021-R18 | exact @ day-021:L6995-L6996 (wrap), typos preserved | matches: agent had ignored the quirk in both places; author separates doc from implementation | follows | none | FAITHFUL |
| B-53 | d013-R14 | exact @ day-013:L998-L999 (wrap) | matches: author asks for the screenshots to be committed and referenced inline | follows | L999-L1000: the stated reason is the agents' missing long-term memory and the double-work it caused — a B-66 datum with an author-verbatim source | FAITHFUL |
| B-53 | d013-R19 | exact @ day-013:L2936-L2938 (wrap) | matches: iPhone tiles vs macOS list view, "Complete" as long-form percentage | follows; `needed-enforcement` accurate | none | FAITHFUL |
| B-53 | d013-R20 | exact @ day-013:L2752-L2753 (wrap), typo "Prvious" preserved | matches: agent's flat-table edit, author restores the segmentation requirement | follows | none | FAITHFUL |
| B-53 | d018-R22 | (second listing) | see B-48 row | follows | none | FAITHFUL |
| B-62 | d021-R14 | exact @ day-021:L8436 | matches: story marked `done`, no spec (L8445-L8451), no implementation (L8458-L8464), no commit (L8469-L8478), status corrected to `backlog` @ L8489 | follows | "Two stories" is verified: `filter-by-reading-status` fails the same check and the agent concludes "Both stories are in the backlog! They're not completed" @ L8508 | FAITHFUL |

Counts: FAITHFUL 45, DISTORTED 10, OVERREACH 3, UNDERREACH 1. No entry has a bad loc — every cited `day-NNN:LNNNN` lands on the described text.

## Notes on non-FAITHFUL rows

**d008-R9 (B-41) — DISTORTED(misattribution).** `because_source: user-paraphrase`, but day-008's conversation does not survive: at L1521 the author records "FUCK! I've been running Code Claude inside Cursor. It's terminal has limited scrollback. The complete history of my conversation with Claude is lost." What remains around L1565 is a diff of a document the agent wrote. The claim itself is right and is carried independently by d009-R20's counts; only its provenance class is wrong.

**d012-R14 (B-41) — UNDERREACH.** The entry's loc is the moment the agent inferred cloud-status *from* plist absence. The rule inverts that into "do not treat absence as evidence", which is correct, but the entry never reaches the turn that established it — the author's "You're jumping to conclusions" (day-013:L866). Cited as-is, the entry looks like agent self-correction when it is an author correction.

**d013-R18 (B-41) — OVERREACH.** "omits cloud-only titles ... even when freshly converted" reads as a mechanism. The transcript's own conclusion is the opposite: after the fresh conversion the author writes "So why the 'Lao Tzu: Tao Te Ching' is not in the All_Books.swift file (even the freshly converted one) is still a mystery to us. Something's fishy there" (day-013:L871-L872). The safe form — plist absence proves nothing — is already in the entry's `because`; the rule sentence goes past it.

**d007-R17 (B-41) — OVERREACH(dropped hedge).** The verbatim quote is three hedges deep: "I'm uncertain about the semantics ... it's probably related to Book's revision ... ? I guess these are store in the SQL db ...?" The rule ("Treat `updateDate` as publisher-revision metadata") and the compiled sentence state it as settled. What the episode actually settled is the decision that followed: the sort feature was deleted as YAGNI because the field's meaning was unknown (day-007:L2739, L2749).

**d014-R3 (B-43) — DISTORTED(misattribution).** The batch at day-014:L735-L764 supplies values, not meanings; the enum glosses are imported from other sessions, and the `because`'s "every earlier single-value story about ZSTATE was contradicted" includes the series story, which was not contradicted until day-015. Labelled `user-paraphrase`, but the sole author turn in the window (L721-L722) says only that Postman is in the cloud and asks for fewer round-trips.

**d012-R11 and d013-R15 (B-43) — DISTORTED.** Both `because` fields say "the two ZSTATE=5 rows in the author's library". There are not two: day-011:L7422-L7628 lists dozens and day-015:L1542 counts 206. Worse for d013-R15, its user-verbatim quote and its rule point in opposite directions — the quote says the ZSTATE-5 rows "look like suggested **books** in a series", the rule says they are "Series containers ... not ... individual books". The container reading is a later author turn (day-015:L1643) the entry does not cite. The compiled B-43 survives both because it carries the disjunction ("Series entity / unowned series book"), which day-015:L1708-L1715 shows is what the data actually holds.

**d015-R7 (B-43) — DISTORTED(misattribution).** The author's verbatim rejection names the replacement: "it must be something with the entries and seriesid". The rule instead promotes the agent's next guess — discriminate by `ZTITLE` — under `because_source: user-verbatim`. Nothing in the window endorses `ZTITLE`; it went into `docs/apple_books.md` (day-015:L2099-L2108) unreviewed.

**d016-R17 (B-43, B-44) — DISTORTED(embellished-because).** "the 3-or-6 widening was explicitly reverted for lacking a failing spec" is a rationale the transcript does not contain. The author's words at day-016:L7169-L7170 are "No, w8, I see that I didn't stop you in time and you've already added 6 to the implementation put it into previous state". The revert was about an unauthorized change, not about spec coverage. The distinction matters because B-44 cites this entry for the claim that display and filter diverge *by design pending a spec*.

**d019-R14, d019-R16 (B-44), d019-R17 (B-47) — DISTORTED(misattribution).** All three cite text inside a Gemini log-analysis document the author pasted at day-019:L794-L854, and all three are labelled `because_source: user-paraphrase`. The author's framing is "I have asked Gemini, which has access to our full console logs ... Following is it's reponse" — relaying, not asserting. For d019-R17 the two supporting locs (L1546, L1602) are the agent's own requirements dialogue and thinking. d019-R17 additionally misses the qualifier the same session produces at L1616-L1617: "Samples can occasionally have progress ... The UI shows some samples with minimal progress, like 'Tiny Experiments' at 1%."

**d009-R23 (B-51) — OVERREACH.** The rule asserts the schema fact ("The book file path is not in the BKLibrary database"); the transcript has only a guess ("might not have path info", day-009:L4459) and a TODO. The compiled rule presents the sentence under a `verified@tree, library.py:73` headline; what is verified@tree is `path=""`, not the absence of a path column.

## Roll-ups (rules where I propose a change)

### B-41 — hedge the two claims the transcripts left open

Two of B-41's sentences are stronger than any episode behind them. "the plist omits cloud-only titles, so absence from the plist proves nothing about library membership" welds an unexplained observation to a mechanism; the author's verdict on that observation is "still a mystery to us. Something's fishy there" (day-013:L871-L872). And "`updateDate` in the plist is publisher-revision metadata, not reading activity" converts a triple-hedged guess (day-007:L2708-L2710) into schema lore. Proposed rewording for those two spans: "*Titles the database knows are demonstrably missing from the plist even after a fresh `plutil -convert swift` snapshot ('Lao Tzu: Tao Te Ching', day-013:L848-L872) — the cause was never established, so absence from the plist proves nothing in either direction: not library membership, not cloud status.*" and "*`updateDate` in the plist is probably publisher-revision metadata rather than reading activity (the author's hypothesis, day-007:L2708-L2710, never verified); its practical consequence is settled — the `sort_by='updated'` feature was deleted as YAGNI because the field's meaning was unknown (day-007:L2739, L2749).*" Both fit under the page's existing observed-2025 stamp; the second adds the only durable decision the episode produced. Separately, the entry `d008-R9` should be reclassified agent-synthesis in the index: day-008's conversation was destroyed (L1521) and only the agent's doc diff survives.

### B-43 — the ZSTATE-5 chronology is inverted

B-43 reads: "earlier research recorded it as 'Series entity / unowned series book' (linked by ZSERIESID …) … but **a later systematic re-probe** observed 5 only as a *second* row for a title that already had a 3, which the series story does not predict." The order is backwards. The duplicate-row observation is day-014:L735-L764 (Attack Surface at 3 and 5). The `ZSERIESID` join — the only probe in the corpus that actually reaches into the structure, returning `Hainish|MultipleAuthors|5|0` plus two unowned Le Guin titles sharing the owned book's series id — is day-015:L1708-L1715, i.e. **later** and considerably more systematic. The two observations are also not in tension in the way the text implies: an owned title carrying a second row at 5 is what a series listing that enumerates unowned members would produce. Proposed rewording: "*ZSTATE 5 is an OPEN HYPOTHESIS, not a verified mapping. Two observations exist and neither settles it: day-014:L735-L764 shows 5 only as a second row for a title that already has a 3 ('Attack Surface'), and the later day-015:L1708-L1715 `ZSERIESID` join shows 5 carried by both the series entity ('Hainish', ZAUTHOR 'MultipleAuthors') and the unowned member titles that share the owned book's series id. `docs/apple_books.md` records the second as a 'Verified ZSTATE Mapping' (written day-016:L5236) and over-claims: the author's own framing leaves the entity/member relation unknown (day-015:L1643-L1645), and the ZSTATE-5 population is ~206 rows dominated by store collections and public-domain classics (day-011:L7422-L7628, day-015:L1542). Treat 5 as unmapped until a live re-census.*" Two smaller corrections in the same rule: the census claim should not say the working set was contradicted by "every single-value story" without noting that "5 means series" was contradicted only on day-015, after day-014; and the closing "the method that produced **every real breakthrough** here" overstates its source — d015-R12 attests one breakthrough (the ZSERIESID join, day-015:L1708-L1715) from one author directive (L1317-L1318). "the method that produced the one real breakthrough here" is what the corpus carries.

### B-44 — the composite predicate's evidence is one episode, not four

`is_sample = ZSTATE = 6 OR ZISSAMPLE = 1` is merged from d012-R13, d013-R17, d019-R14 and d018-R16, but the first three are one observation: the author's "Snowcrash is a Book Sample, as is Tiny Experiments" (day-012:L2975) and the two probes returning `ZISSAMPLE: 0` (L2986, L2997). d013-R17 is that exchange replayed inside day-013's recap; d019-R14 is Gemini's summary of it. Only d018-R16 is independent, and it is the strongest turn in the set ("Why do you insist on ZISSAMPLE=1? That's edge case. We want books with ZSTATE=6!", day-018:L2198). Separately, the sentence "Per-title sample observations flipped between probes in 2025 (the same title read `ZISSAMPLE 0` one session and `1` another)" describes as instability what the preceding sentence has just explained as the lifecycle: "Tiny Experiments" read `(6,0)` on day-012 and `(1,1)` after the author opened it. Proposed rewording of that span: "*Per-title values are time-varying, not stable facts: 'Tiny Experiments' read `ZSTATE=6, ZISSAMPLE=0` (day-012:L2986) and later `ZSTATE=1, ZISSAMPLE=1` after being opened — the lifecycle above is the mechanism. Any per-title claim therefore needs a freshness stamp and a re-run query, never a citation of notes.*" And the divergence clause should not rest on d016-R17's invented rationale: the 3-only filter survives because the author reverted an unauthorized widening (day-016:L7169-L7186), not because a spec was found wanting.

### B-47 — carry the counter-observation from the same session

B-47 hedges correctly ("appears never to populate") but omits the qualifier that appears 700 lines later in the very session it is mined from: "Samples can occasionally have progress, contrary to my initial assumption. The UI shows some samples with minimal progress, like 'Tiny Experiments' at 1%. Our test fixtures consistently show 0% progress, but real-world scenarios might differ" (day-019:L1616-L1617). Proposed addition to the rule: "*Counter-observation from the same session (day-019:L1616-L1617): the UI renders some cloud samples at 1%. B-53 records that as an Apple UI quirk not to be replicated; the database-side claim (`ZREADINGPROGRESS` stays 0.0 for samples) is what remains untested.*" Provenance should also be restated: all three of this rule's locs are agent text — a Gemini log summary the author relayed (L794-L854) and the agent's own requirements dialogue (L1546, L1602) — so `user-paraphrase` in the index should read agent-synthesis.

### B-51 — split the stamp

The rule's headline reads "(verified@tree, library.py:73)" and its body asserts "the database has no usable path". Only the first is verified: `_row_to_book` sets `path=""` and says why (day-020:L10258; `path` made `NotRequired` at L10416). The schema claim is a guess never checked — "we query BKLibrary database which **might** not have path info" (day-009:L4459) — and the author's instruction was precisely to mark it as an open question rather than resolve it ("Seems like a good question... we might not have a good answer yet -- in such cases a FIXME/TODO comment might help us keep track of an unfinished issue", day-009:L4436-L4438). Proposed rewording: "*`_row_to_book` sets `path=""` pending a `ZASSETID`-keyed correlation with Books.plist (verified@tree, library.py:73; `path`/`updated` are `NotRequired` on `Book` for the same reason). Whether the database has any usable path column was never checked — it was left as an acknowledged TODO at the author's direction (day-009:L4436-L4459) — so treat it as needs-live-DB, not as an established schema fact.*"

### B-52 — one clause has no source in its row

"no print-debugging through production code" is corpus-true but none of B-52's listed sources (d016-R14, d012-R16, d014-R5, d014-R8, d015-R12) contains it. Its actual sources are `d015-R1` ("DO NOT FUCKING TOUCH OUR IMPLEMENTATION for exploratory probing of databases", day-015:L897-L926) and `d014-R6` (day-014:L199-L204) — both `skill_target: tdd-bdd`, both already compiled into T-96. Either cite them in B-52's index row as a deliberate cross-skill echo, or drop the clause and let B-52's first sentence point at T-96. Leaving it uncited makes the rule look better-sourced than its row shows.

### B-53 — restore the named speculative sections

d008-R11's rule is more specific than the compiled sentence: it names "Edge Cases", NULL-handling and `ZCONTENTTYPE` as the parts of `docs/apple_books.md` to read as speculation, and records that the speculative half "later seeded a real defect". B-53 generalizes to "a running record of guesses", which is true but unactionable, and only `ZCONTENTTYPE` survives (via B-45). Proposed addition to B-53's first sentence: "*— its 'Edge Cases' and NULL-handling sections, like its `ZCONTENTTYPE` mapping (B-45), are speculation the document presents in the same voice as its observations.*"
